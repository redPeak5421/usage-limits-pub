# DEVLOG · 问题点记录

按目标要求，记录「开发 → 模拟器验证 → 修复」循环中的每一个问题点。
状态图例：✅ 已修复 · 🔄 处理中 · 📌 已知边界（无法在无真实账号/当前环境下闭环，已给出说明）

| # | 阶段 | 现象 | 原因 | 处理 | 状态 |
|---|------|------|------|------|------|
| 1 | 工具链 | `simctl list runtimes` 为空，iOS 18.5 / 26.4 / 26.5 设备全部 Unavailable，无法启动任何模拟器 | 本机没有任何 iOS 模拟器运行时磁盘镜像（Total Disk Images: 0）；Xcode 26.6 只带 SDK（iphonesimulator26.5）不带运行时 | 后台执行 `xcodebuild -downloadPlatform iOS` 下载运行时；期间所有编译验证改用 `-destination 'generic/platform=iOS Simulator'`（不依赖运行时） | 🔄 |
| 2 | Core 单测 | `testGrokRateLimitsAndPlan` 断言失败：算出 9.999…% ≠ 10% | `(1 - remaining/total) * 100` 的二进制浮点尾差 | GrokParser 中已用百分比统一四舍五入保留一位小数 | ✅ |
| 3 | 编译 | `xcodebuild -destination 'generic/platform=iOS Simulator'` 报「iOS 26.5 is not installed」拒绝构建 | Xcode 26 在模拟器运行时未安装时，连 generic 模拟器 destination 都不予解析 | 等待运行时下载期间，改用 `xcrun -sdk iphonesimulator26.5 swiftc -typecheck -target arm64-apple-ios17.0-simulator` 对全部源码做类型检查（App+Widget 首轮即零错误） | ✅ |
| 4 | 编译 | `xcodebuild -target AIUsage -sdk iphonesimulator26.5`（旧式 target 构建）报 unable to resolve module dependency: 'AIUsageCore' | 旧式 target 构建不走 workspace，无法解析本地 SwiftPM 包依赖 | 弃用该路径；正式构建等运行时就绪后用 `-scheme` + 具体模拟器 destination | ✅ |
| 5 | 端到端实测 | ChatGPT 疑似「未登录却显示已登录」，连续三轮收紧判定仍为 ok | 深挖后定性反转：session 里是**用户本人的真实会话**（email 为本人 gmail、2120 字节 token）——模拟器无人登录却有会话，判断为本机网络代理层对 chatgpt.com 自动注入会话所致；判 ok 是真阳性 | 判定逻辑仍收紧为「session 在场时唯 email 为证，accounts_check/wham 只作 session 缺失时的兜底」并保留 3 个匿名形态回归测试（`testOpenAIAnonymousSessionNeedsLogin` 等）——对无代理注入的普通环境这是必要防御 | ✅ |
| 6 | 端到端实测 | Grok 未登录也显示为已登录「Grok」，带 2/2 额度 | grok.com 给匿名访客提供游客额度：`rest/rate-limits` 未登录也返回 200（grok-4 为 4 小时窗口 2 次），`rest/subscriptions` 返回空 | 保留游客额度展示（数据真实有用），套餐标注「游客额度」+ `isAnonymous` 字段，卡片保留「登录查看订阅额度」按钮；新增回归测试 `testGrokAnonymousQuotaLabeled` | ✅ |
| 7 | 验证工具链 | 用 `plutil -remove` 清模拟器里 App Group 的诊断日志无效，旧日志一直回魂 | cfprefsd 缓存了该偏好域，App 进程下次写入时把内存旧值整体写回 | 改用 `xcrun simctl spawn booted defaults delete <suite> <key>`（走 cfprefsd 的正规删除） | ✅ |
| 8 | 端到端实测 | Grok 匿名探测从第 3 轮起被站点限流（subscriptions 403、rate_limits 部分组合被拒），快照回落为「未登录」 | 短时间多轮匿名探测触发 grok.com 风控 | 防御逻辑按设计工作（无数据→needsLogin，不误报）；真实登录携带会话 Cookie 后不受此影响，记为已知边界 | 📌 |
| 9 | 端到端实测 | Claude 探针从 403(JSON) 变成 404 + 75KB HTML（Webflow 营销页） | claude.ai 根路径未登录时 302 到 claude.com 营销站，隐藏 WebView 源漂移到 claude.com，相对路径 `/api/*` 全部打到错误域名 | 新增 `ProviderID.probeURL`：Claude 探针页固定加载 `claude.ai/login`（登录前后都停留在 claude.ai 同源） | ✅ |
| 10 | 端到端实测 | 真实 ChatGPT 会话下 `accounts/check`、`wham/usage` 仍返回 401 | 该会话的 accessToken 对这两个 backend-api 端点无效（可能需要额外设备头/端点版本变化，或代理注入的会话形态特殊） | 已登录时卡片如实显示「已登录，但官方未提供数值额度」+ 套餐 ChatGPT；端点漂移由诊断日志可查，记为已知边界（后续可按真机诊断加端点） | 📌 |
| 11 | 真机校准 | Grok 卡片列出 grok-4/grok-3「标准/推理」，套餐显示 `Subscription_Tier_Grok_Pro` | 探针仍按 2025 的 grok-4/grok-3 组合请求；官网现行档位是 auto/fast/expert/heavy，订阅枚举也换成 `Subscription_Tier_*` | 探针改为四档；解析层映射「自动/快速/专家/Heavy」与套餐枚举 | ✅ |
| 12 | 真机校准 | 付费账号卡片展示「自动 150/150」等 2 小时次数，套餐写成 Pro；官网 Settings → Usage 只有「每周 SuperGrok Heavy 限额」百分比 + Grok Build / Imagine | `/rest/rate-limits` 是短期限流，不是普通用户可见维度；周额度在 `GetGrokCreditsConfig` / credits JSON | 付费套餐只展示本周限额百分比与产品占比；套餐名 SuperGrok Heavy；150/150 仅游客兜底 | ✅ |
| 13 | 真机实测 | ChatGPT 登录页强推通行密钥验证，走到「请确保蓝牙已开启且设备相距不远」后失败，登录卡死 | 无浏览器专属权限的 WKWebView 拿不到 iCloud 钥匙串通行密钥；站点检测到 WebAuthn 便走跨设备/蓝牙流程，必然失败 | **初版错误**：把 `PublicKeyCredential` 整段藏成 `undefined`，导致 #40。**现行做法**：只 stub 可用性探测为 false，并拒绝 `credentials.get/create` 的 publicKey，全局对象必须保留。脚本在 `LoginWebViewScripts.hideWebAuthn`。硬约束见 `.cursor/rules/login-webview.mdc` | ✅ |
| 14 | 真机实测 | 官网确认是 ChatGPT 免费版，卡片却显示「ChatGPT Pro $200」+ 订阅状态「有效 · 到期：已重置」 | `accounts/check` 里残留**已到期的历史订阅记录**（`subscription_plan: chatgptproplan`、`has_active_subscription: false`、`expires_at` 在过去），解析器只认 `subscription_plan` 就当现行套餐；且订阅指标行的「有效」是硬编码 | 只有 `has_active_subscription` 非 false 且未过期的记录才算现行套餐，否则回落 `account.plan_type`；过期订阅不再产生「订阅状态」指标。新增回归测试 2 条（过期 Pro 回落 free、active 标记但日期已过期同样忽略） | ✅ |
| 15 | 真机实测 | 登录完成后 sheet 不自动关闭，需要手动点「检测登录」 | 旧逻辑只在 WebView `didFinish` 后延迟 2 秒探测一次；OAuth 回跳/站内 JS 导航不触发 didFinish，登录完成探测不到 | LoginSheetView 改为打开期间每 4 秒后台轮询（`.task` 随视图取消），探到即提示并自动关页；手动按钮保留，自动轮询不闪动按钮 spinner | ✅ |
| 16 | 真机校准 | Claude 卡片缺官网「Weekly limits」下 All models 与 Fable（按模型）两条周限额的区分；`resets_at` 微秒时间戳解析失败丢重置时间 | usage 响应的 `limits[]`（含 `weekly_scoped` + `scope.model.display_name`）未解析；`ISO8601DateFormatter` 只认毫秒级小数，真机返回 6 位微秒 | 接入 `limits[]`（session/weekly_all 与顶层窗口按 id 归一去重，weekly_scoped 单列）；计量名改用官网原名 Current session / All models / Fable；`JSONHelp.date` 截微秒到毫秒。真机报文进 fixture + 回归测试 | ✅ |
| 17 | 新增服务商 | 接入 Cursor（cursor.com/dashboard/spending） | 端点无官方文档，取社区逆向口径（dmwyatt/cursor-usage）：`GET /api/usage-summary`，Cookie `WorkosCursorSessionToken` | 全链路接入（ProviderID/探针/CursorParser/价目/演示/小组件），计量按官网 Spending 页三条对齐：Cursor Models（`autoPercentUsed`）/ Other Models（`apiPercentUsed`）/ Grok Bot（字段名待真机日志校准，先按键名含 grok 防御式探测）；老形状回退 Included 总量。详见 `providers/cursor.md`。真实响应待真机验证 | 🔄 |
| 18 | 体验目标 | ① 无重置信息的指标行高不一致；② 标题改 Usage Limits；③ 需要语言选项（中/英/日/法/俄，默认跟随系统、不匹配用英语）；④ 需要自动刷新档位（0/5s/10s/30s/1m/10m/30m/1h 滑杆） | — | ① MetricRowView 底行 `minHeight: 14` 恒占位；② 标题固定 Usage Limits；③ 自建 L10n 表（Core `Localization.swift`，App 内即时切换免重启，`\.appLanguage` 环境注入 App+小组件，相对时间同步本地化）；④ SharedStore 持久化 + AppState 前台循环刷新 + 设置页系统 Slider（iOS 26 自带 Liquid Glass），0 档关闭。注：小组件在桌面配置器里的名称/描述暂未随语言切换（WidgetKit 静态注册限制） | ✅ |
| 19 | 计价校准 | 订阅价格未区分计费来源：iOS 内购价高于官网（Claude Max 20x 内购 $249.99 vs 官网 $200，Max 5x $124.99 vs $100） | 价目表只有官网价；快照没有计费来源字段 | PlanCatalog 增加 App Store 价目（仅收录已证实档位，缺失回落官网价）；ProviderSnapshot 增加 `billingSource`；ClaudeParser 按 billing/payment/platform 相关键值含 apple/ios/iap 防御式探测（字段名待真机 organizations 报文校准）；卡片价格徽章按来源取价 | 🔄 |
| 20 | 体验目标 | ChatGPT 卡片单列「订阅状态 有效/到期」一行，其他服务商都没有，口径不一致 | 该行是 OpenAI 解析特有的附加项，不是用量计量；套餐徽章+价格已表达订阅信息 | 从解析、演示数据、渲染特判（hasUsage/到期前缀/「有效」值）全链路移除；测试同步改为断言不存在 | ✅ |
| 21 | 体验目标 | ① 主页/设置需长按拖动排序且联动；② 标题去箭头但保留点击折叠、恢复滚出后缩小置顶居中的过渡；③ 滑杆拖动卡顿；④ 标签字号不一致、文本换行 | ② 自定义页内标题替换系统大标题后丢了过渡；③ 拖动每帧写 @Published+UserDefaults+重建刷新任务；④ minimumScaleFactor 使个别文本单独缩字 | ① 一份全局顺序（SharedStore.providerOrder）：主页 onDrag/onDrop 只移动被拖卡片（插到可见前驱之后、排在紧邻关闭项之前），设置页 DisclosureGroup 内 onMove 直接改全局顺序，小组件同步该顺序；语义两用例进单测。② 滚动探测页内标题滚出后导航栏内联标题淡入（同样可点击）。③ 拖动只动本地档位、松手提交。④ 移除卡片内自动缩字，同类标签恒同字号 + lineLimit(1) | ✅ |
| 22 | 真机实测 | 桌面小组件全部显示「未登录」，App 内却能正常显示并刷新用量 | 两个 `.entitlements` 里 App Group 是空数组（`<array/>`）：App 进程 `UserDefaults(suiteName:)` 拿不到共享容器回落 `.standard`，自己读写一致所以正常；小组件进程各用各的存储，永远读不到快照。模拟器不强制 entitlements，此前验证未暴露 | 两个 entitlements 补上旧 Bundle ID 对应的 App Group（project.yml 的 properties 本来就有，生成文件漏了）。需真机重装后验证 | 🔄 |
| 23 | 体验目标 | 设置页服务商长按拖动排序卡顿、不流畅 | ① DisclosureGroup 内嵌 ForEach 的行不是 List 直属行，系统重排动画在 Form 里表现很差；② 每次排序提交 `applyOrder` 同步做「逐家 JSON 解码快照 + WidgetCenter 跨进程刷新」，主页拖动时 dropEntered 每过一张卡触发一次，逐帧掉帧 | ① 设置页改为普通行 + 条件展开（点击带 chevron 的头行折叠/展开），重排走 List 原生行拖动；② `applyOrder` 不再重载快照（顺序不影响快照缓存），小组件刷新合并 0.6s 防抖（主页与设置页拖动同时受益） | ✅ |
| 24 | 体验目标 | 卡片折叠/展开时套餐徽章与指标行「突然出现/消失」，无位移动画；折叠态默认显示的是最短窗口（Current session）而非周额度 | 折叠/展开是两套独立视图分支，SwiftUI 认为是不同身份，只能整块淡入淡出；折叠摘要取 `primaryMetric`（列表首条，一般是 5 小时窗口） | 套餐徽章在头部与展开紧凑行之间用 `matchedGeometryEffect` 位移过渡（价格徽章缩放淡入）；折叠/展开共用同一个 ForEach（行 id 稳定），切换时折叠那条原地滑到新位置、其余行淡入；折叠摘要改取新增的 `longestWindowMetric`（重置时间最远＝窗口最长，一般即周额度，并列取靠前，无重置时间时回退周总量/主指标），回归测试 2 条 | ✅ |
| 48 | 用户反馈 | TestFlight 用户交来的诊断日志只有「HTTP 200，N 字节」，分析不出接口漂移原因 | 所有 `*.head=` 报文预览都包在 `#if DEBUG`，Release 构建不写；诊断页也没有复制 / 分享入口 | 新增 `DiagnosticRedactor`：每个探针一行 `HTTP 状态 + 字节数 + 脱敏 JSON 预览`（token / cookie / session / email / uid / 签名类键整值打码，≥40 字符无空格串与邮箱打码，长数组只留 5 项，700 字符截断），每次解析一行 `*.parsed=`（状态 / 套餐 / 每条指标 id、标签、百分比或金额、距重置分钟）；Release 同样记录；环形缓冲 200 → 500 行；诊断页加「复制」「分享」并按类型着色。单测 4 条锁脱敏与摘要 | ✅ |
| 49 | 真机实测 | ChatGPT 卡先是两个「Codex 周窗口」100% / 0%，后两行都 0%，官网实际 99% | 用户交来 0.2.160 日志：wham 现网形状为主额度 `rate_limit` + `additional_rate_limits[{limit_name:"GPT-5.3-Codex-Spark", rate_limit:{…}}]`。附加限额的父级键也叫 `rate_limit`，被当成主分组，id 与主额度同名撞车，排序后主额度被去重丢掉，剩下的是 Spark 的 0% | 按顶层结构显式解析：主额度 / 代码审查 / `additional_rate_limits[]`（以 `limit_name` 命名，id `additional.<name>.<窗口>`）/ 未知分组兜底，各分组 id 互不相同；0% 附加限额默认不露出；`providers/chatgpt.md` 记录完整形状；回归测试用真机形状锁定 [99, 99, 0, 0] | ✅ |
| 50 | 用户要求 | 对外联系邮箱只能是 canonforge5421@gmail.com | TestFlight 测试员看到的邮箱来自 App Store Connect「TestFlight 测试信息 → 反馈邮箱」，不在 ipa 内 | 新增 `AppContact.feedbackEmail` 唯一来源，设置页版本行下加「反馈邮箱」mailto 行；git 作者邮箱统一为该地址；CLAUDE.md 隐私节写入禁令。ASC 反馈邮箱需在网页改 | ✅ |
| 25 | 体验目标 | 设置需要主题选项：深色 / 浅色 / 跟随系统 | — | Core 新增 `AppTheme`（system/light/dark，SharedStore 持久化 + 回归测试）；设置页「主题」分段选择器（五语言文案）；根视图 `preferredColorScheme`（system 传 nil 不覆盖系统外观）。小组件底色仍随系统外观（WidgetKit 不受 preferredColorScheme 影响） | ✅ |
| 26 | 体验目标 | 2×4 / 4×4 总览小组件行数少时内容挤在上部、下方大片空白（真机 4×4 截图） | `MediumUsageView` 行高按内容自然高度 + 固定 spacing，VStack 不撑满容器 | 每行 `.frame(maxHeight: .infinity)` 均分整块小组件高度：行少时自动变高、排列均匀；行数上限（medium 5 / large 11，每行 ≥ 约 30pt）保住最小可读行高，超出照旧截断提示「还有 N 项」 | ✅ |
| 27 | 验证工具链 | `simctl spawn booted defaults write` 写入旧 Bundle ID 的 App Group 后 App 读不到 | entitlements 修复（#22）后 App 的 suite 解析到 **App Group 共享容器**（`Containers/Shared/AppGroup/<uuid>/`），而 spawn defaults 按域名写的是用户级 Preferences，两者不再是同一文件（修复前恰好同路径，DEVLOG #7 的老办法因此失效） | 改用容器内 plist 全路径作 domain：`simctl get_app_container booted <bundle> groups` 拿路径，再 `simctl spawn booted defaults write <容器>/Library/Preferences/<App Group>.plist <key> <value>` | ✅ |
| 28 | 新增平台 | 需要 Apple Watch 应用：健身圆环式用量、横滑切换服务商、纵滑进设置、开关与 iPhone 双向同步 | 手表与 iPhone **不共享 App Group**，数据必须走 WatchConnectivity | 新增 `AIUsageWatch` 目标（watchOS 10，XcodeGen 自动 Embed Watch Content）：`WatchStore` 收 iPhone 推送的 applicationContext（快照/启用/顺序/语言/演示，snapshot JSON 编码，无凭据）落本地缓存离线可看，表端开关 sendMessage/transferUserInfo 回传；iPhone 端 `WatchSync` 在快照落盘/开关/排序/语言/演示/退出登录时整体推送。UI：外层 `.verticalPage` TabView（圆环页 / 设置页），内层 `.page` TabView 每页一个服务商——品牌色同心环（外环最实，透明度阶梯区分）+ 图例逐条写明限额级别名与阈值配色百分比；设置页开关列表与 iPhone 联动。共用件抽到 `SharedUI/Branding.swift`（语言环境键/品牌色/阈值色），Core 包平台加 watchOS。调试参数：`--demo` / `--provider <id>` / `--open-settings` | ✅ |
| 29 | 验证工具链 | 模拟器 iPhone→Watch 推送报 `WCErrorCodeWatchAppNotInstalled`（表端却显示 companionAppInstalled: YES） | `simctl install` 手动装表侧 App 后，iPhone 端 wcd 的配对注册表不知道对端已装；simctl 也不会像 Xcode 那样经 iPhone App 自动安装内嵌手表 App | 顺序修正后打通：**表侧装好 App → 重启手表模拟器 →（先后）启动表端与 iPhone 端 App**，注册表在手表启动时同步，推送即达 | ✅ |
| 31 | 新功能 | 需要提醒系统：额度百分比阈值、套餐到期前 N 天、额度重置（含供应商主动重置检测），全部可单独开关、可配提前量，设置二级菜单，支持手表推送 | App 无服务器，只能本地通知；阈值/回满检测依赖新数据，而探针只在前台可靠 | 三层设计：① Core 纯决策器 `NotificationDecider`（阈值穿越=旧<阈值≤新、回满=最长窗口指标从 >0 归 0，去重键含 resetsAt，窗口重置自动重新武装，11 条单测）；② `NotificationManager` 即时投递 + 用 `UNCalendarNotificationTrigger` 预排「到期前 N 天」「到点重置」（App 不在前台也能弹）；③ BGAppRefreshTask 尽力而为（用户拍板），后台探针非 ok 结果不落盘防止把已登录刷成未登录。到期时间新增 `planExpiresAt`（现仅 OpenAI accounts_check 可证实）。手表推送零开发：iPhone 锁屏时系统自动转发本地通知到已配对 Watch。真机通知到达与后台刷新频率待人工观察 | 🔄 |
| 30 | 体验目标 | ① 手表圆环用品牌色透明度阶梯区分环，色差太小看不出图例对应哪一环；② 手表设置页需要长按拖动排序并与 iPhone 联动 | 三环同色仅靠透明度确实难分辨；表端此前只有开关同步，没有排序回传 | ① 改为「选中式」纯同心圆：外环 = 重置时间最远（周期最长）的限额、默认选中，点击任一环（按距最近环中心线命中，免精确点击）即高亮该环、其余淡化，环外侧显示该限额级别名 + 百分比 + 重置时间；删掉图例列表。② 设置页 ForEach 加 onMove 长按拖动，`moveOrder` 改本地并回传 `setOrder` 消息，iPhone 端 `AppState.setOrder` 补齐缺失项后走 `applyOrder`（首页/设置/小组件/回推手表全联动）。新调试参数 `--select-ring <id>`；模拟器截图验证默认外圈选中态与预置内圈选中态 | ✅ |
| 32 | 新增服务商 | 接入 DeepSeek 预充值、智谱 Coding Plan CN、Kimi Code Plan、MiniMax TokenPlan | Chrome DevTools MCP 默认隔离配置无登录 Cookie；用户开启远程调试后经 DevToolsActivePort WebSocket 抓到真实接口 | 四家目录+脱敏 fixture+解析器+探针+演示；DeepSeek 专用折叠两金额/展开时间维度+API Key 图表。智谱 `percentage` 已是 0–100，误走 JSONHelp.percent 会把 1% 放大成 100%，已按 Cursor 同口径钳制 | ✅ |
| 33 | 登录闭环 | 新四家在内置浏览器登录成功后不自动关页，手动关掉首页仍是未登录 | 用量接口要 `Authorization`，Token 在 localStorage/可读 Cookie：DeepSeek `userToken.value`、智谱 `bigmodel_token_production`、Kimi `access_token`。探针只 `credentials:include` 时 DeepSeek 200 Missing Token、智谱 1001、Kimi 401，解析成 needsLogin | 探针从同源存储取出 Token 再带 Bearer；MiniMax 补 `x-group-id`；DeepSeek 时段请求改为并行以免 30s 超时；登录页改到 Usage/Console 同源；sheet 消失后再 refresh 一次 | ✅ |
| 34 | MiniMax / Kimi | ① Kimi `/code/console` 登录页无二维码；② MiniMax 折叠只剩周限额、展开只剩积分；③ 无套餐价 | ① 扫码在 `/code`；② 官网三条（5h/周无限/视频）0% 被 hasUsage 藏掉，积分因 amount>0 反成唯一展开项；③ 套餐名误从积分包 `Token Plan Credit` 推断，对不上标价表 | Kimi 登录改 `/code`；三条用量 `pinned`；折叠摘要改 5h；周无限不造重置时间、数值 ∞；combo 年/月卡「续订套餐」映射 Token Plan Max 年 ¥1,190 | ✅ |
| 35 | 体验目标 | 展开卡片要按订阅形式显示周期标签（月/年）和对应金额 | 价目表只有月价；MiniMax 把「 年」写进套餐名；其它家没有 `billingCycle` | 快照增加 `BillingCycle`；解析器从接口字段/账期跨度读取；`PlanCatalog` 按周期取价；展开行在金额前画「月」「年」胶囊 | ✅ |
| 36 | 体验目标 | ① DeepSeek API Key 列表过长撑开卡片，顺序不是最近使用；② MiniMax 5h 显示「剩 -1/-1」；③ 首页大标题与设置按钮错位，顶部空一块 | ① Key 按名字排、全部展开；② 共享额度 count=-1 被当成剩余次数；③ 页内大标题 + 空导航栏各占一行 | ① last_use 倒序 + 固定 5 行内部滚动；② -1 计次丢弃，只画百分比；③ 标题与齿轮同排进 inline 导航栏 | ✅ |
| 37 | 计价校准 | 智谱 / MiniMax 年、月标签错，年付显示成月价 | 智谱只认 billingCycle 字段、缺了按 productId 对照 glm-coding 商品表；MiniMax 标题已是 TokenPlanMax-年度会员 仍可能落到月卡 | 智谱用 productId+包年计划+有效期跨度判定周期，SKU 标价进 PlanCatalog；MiniMax 认年度会员/续订/cycle_type=3，年付取 ¥1,190 | ✅ |
| 38 | 体验目标 | ① MiniMax 5h 要按官网 0%/100%；标签色要官网渐变粉；② 所有服务商名称前圆点改纯图形 logo | 5h 只画了单百分比；MiniMax 品牌色是橙红；抬头用色点 | 解析器产出 displayValue；品牌色/徽章用 #E21680→#FF633A；SharedUI 商标图替换所有圆点 | ✅ |
| 39 | 真机实测 | Claude 点 Google 立刻红框 *There was an error logging you in*，电脑浏览器正常 | Claude 用 `window.open` 走 Google OAuth；iOS `WKWebView` 默认 `javaScriptCanOpenWindowsAutomatically = false`，且未实现 `createWebViewWith`，弹窗被丢。UA 曾写死 iOS 18.5，与真机 26.x 不一致会被风控打回 | 打开 JS 弹窗；`WKUIDelegate` 用 WebKit 给定 configuration 建弹窗（保留 opener，禁止改当前页 load）；`SafariUserAgent.make` 按设备系统版本拼 Safari 形态 UA。`LoginConfirmTests` 锁契约。**以后禁止再关弹窗或写死旧 UA** | ✅ |
| 40 | 真机实测 | ChatGPT 登录页 Google / Apple / 电话三个按钮看起来能点、点了没反应；邮箱「继续」正常 | #13 初版把 `PublicKeyCredential` 设成 `undefined`。三按钮共用 React `onClick`，依赖该全局仍在；对象没了就空转、无报错。对照：去掉该注入后 `button.click()` 会跳到 `auth.openai.com/...&connection=google-oauth2` | 改为 stub 可用性 + 拒绝 publicKey，不得再删全局。`LoginConfirmTests` + JSContext 锁「typeof 仍是 function」。**以后禁止再藏掉 `PublicKeyCredential`** | ✅ |
| 41 | 真机实测 | ChatGPT $100 Pro 账号卡片显示「ChatGPT Pro $200」 | 解析器按 `subscription_plan` 含 `5x` 识别 $100 档，属推测值；现网 $100 档的标识是 OpenAI 内部命名 **Pro Lite**（`chatgptprolite` / `PROLITE`），不含 `5x`，落到 plain Pro | `planLabel` 先匹配 `pro`+`lite` 或 `pro`+`5x` → ChatGPT Pro 5x（$100）；`providers/chatgpt.md` 记录实际值；回归测试 `testOpenAIProLitePlanMappedToPro5x` | ✅ |
| 42 | 真机实测 | 误触长按卡片后卡片一直深色（0.6 透明度）不恢复 | `onDrag` 把 `draggingAccountID` / `draggingProvider` 置为当前卡，只有落到另一张卡触发 `performDrop` 才清空；原地松手、拖到间隙 / 页脚放手、系统打断都不会调用，状态残留 | ① 滚动内容加兜底 `onDrop` 接住卡片外放手；② `DragSessionItemProvider`（NSItemProvider 子类）在拖动会话结束被释放时清状态，覆盖取消场景；③ 退到后台也清 | ✅ |
| 43 | 模拟器日志 | 即梦登录后卡片只有「剩余积分 —」，从不显示积分 | 诊断日志每轮 `isLogined=false`、`hasSigner=false`、额度/流水 `ret=1014 system busy`：探针只认页面 `webSignBody`，匿名 SSR / 冷页里它从不挂上，等满 10s+8s 后裸发，commerce 必回 1014。抓包响应带 `sign` 字段与开源反代（jimeng-free-api）都证实该接口需 `Device-Time` / `Sign` / `Sign-Ver` 头，公开算法 `md5("9e2c|<路径末7位>|7|5.8.0|<秒>||11ac")` | `JimengSession.signPayload` 拼签名串（单测锁定），`WebViewFetcher` 用 CryptoKit MD5 注入 `deviceTime` / `signCredit` / `signHistory`，探针在页面签名器缺席时用 native 头兜底，query 补 `aid/device_platform/region`；等待缩到约 3s。`providers/jimeng.md` 记录签名算法。模拟器实测：credit `ret=0`，卡片 179 / 0 / 149 / 30 + 流水，与抓包一致 | ✅ |
| 44 | 体验目标 | 自定义 provider 卡折叠只露一格「余额 168.80」信息太少；报文形状不固定，向导只按路径末段起名、不识别已用 / 余额 / 百分比 / 时间 | 解析层只拍扁数字叶子，没有语义；展示层只能按展示名含「已用」「余额」猜占比 | 新增 `CustomFieldSemantics`：按键名 / 原文 / 兄弟 `currency` 推断角色与币种，支持 `$168.80` / `42%` / `1,024` 字符串叶子，向导按分数自动勾选 ≤ 4 条、按父路径分组、角色徽章可改；字段 / 指标带 `role` / `kind`（旧模板兼容）；`CustomUsageDisplay` 推导主指标 / 进度条来源 / 时间戳分组 / 千分位；卡片折叠 = 主指标大号 + 两格 + 细条，展开 = 着色主块 + 2 列网格 + 到期行。模拟器注入快照验证两态 | ✅ |
| 45 | 体验目标 | 卡片折叠 ⇄ 展开时，折叠态的数字在展开态闪现到新位置，没有过渡 | 折叠 / 展开是两棵互斥视图树，同一指标的 Text 是不同节点，SwiftUI 只做淡入淡出 | `ProviderCardView` 建共享 `@Namespace geometry` 传给自定义 / 即梦 / DeepSeek 卡体；同一指标的数字与标签两态各挂 `matchedGeometryEffect(id: "value.<id>" / "label.<id>")`，自定义卡进度条轨道挂 `"gauge"`，仅一态存在的容器用 `.transition(.opacity)`；通用指标行与套餐徽章此前已靠稳定 id / `badgeNS` 原地滑动。录屏抽帧（20fps）确认即梦四档数字与 DragonCode 主指标均为连续位移 / 缩放 | ✅ |
| 46 | 真机实测 | Grok 周额度出现「分类 7」分组；网页端叫 App Builder，iOS 端叫应用构建器 | `GrokWeeklyProduct.label(forCode:)` 只映射 0–6；App Builder 是与 Grok Build（code 2）并列的新产品（Build Mode），code 7 无映射落到兜底。按名匹配时 "APPBUILDER" 又会被 `contains("BUILD")` 误归 Grok Build | code 7 → App Builder（沿用「品牌产品保留官方英文」规则，「应用构建器」只是 iOS 本地化）；按名匹配先判 APPBUILDER / 应用构建；`providers/grok.md` 补编码表；回归测试 1 条 | ✅ |
| 47 | 真机实测 | ChatGPT $100 Pro 只有周额度且已用尽，卡片却显示「Codex 5 小时窗口 100%」+「周窗口 0%」 | 现网 wham 时长字段是 `limit_window_seconds`，解析器只认 `window_minutes`，拿不到时长就按 `primary`→5 小时 / `secondary`→周 硬猜；该套餐唯一的周窗口正好在 `primary_window` | 时长兼容 `limit_window_seconds`；窗口类型只按时长判定，缺失再按距重置时间推断，不看路径名；`code_review_rate_limits` 单列并区分 id；诊断加 `openai.wham_usage.head`；`providers/chatgpt.md` 更新形状；回归测试 2 条 | ✅ |

## 验证轮次记录


### 2026-09-05 · 0.4.545 · Grok 网页登录与中转页误判

- 用户确认：原生 Grok App 被唤起但未完成回调；23:53 新日志已解析 SuperGrok Heavy、本周限额 / Grok Build 各 2%，登录窗口仍显示「请先在官网完成登录」。可选 `credits` 404 不影响有效订阅和周额度。直接原因是登录窗口要求可见 URL 回到 Grok，忽略了中转页仍停留时已经生效的官网会话。
- Grok 从 `grok.com/` 的网页客户端发起认证。普通导航 / 新弹窗均拦截 App scheme 与商店链接，提供「继续网页登录」；自有站点主框 GET 点击 / 跨域回跳用原 WebView 重载完整请求，单次重放标记避免循环。Google / Apple HTTPS OAuth、POST 和 iframe 不改写；保留 WebKit 弹窗 configuration / opener，不调用私有策略枚举。
- Grok 可见用量页未就绪时，用同账号 Cookie store 的离屏官网页执行新探针；仍要求本轮 `ok` 且非游客才弹确认，不从历史缓存、Cookie 是否存在或原生 App 返回推断成功。Cursor 的可见 Dashboard 约束保持原样。两项修复累计 z +2 到 **0.4.545**。
- 新增网页导航 / 域名边界 / 登录中转页回归，以及付费 rate-limits + 合成周额度 2% + 可选 404 的登录确认用例；无用户交易号或真实身份入夹具。先记录缺失策略与中转页用例失败，再通过全量 **1,084 项、0 失败**（含 `LoginConfirmTests`）。XcodeGen 与 Debug App scheme 构建通过，代码复核未发现重要问题。日志 `/tmp/grok-web-tests.log`、`/tmp/grok-web-build.log`。
- 独立模拟器 0.4.545：Grok 首页 → 网页「登录」→ xAI「您正在登录 Grok」→ 邮箱输入页，均留在内置 WebView。官网自带的「打开 Grok」横幅仍可出现，使用关闭按钮后网页登录入口可见；防止原生跳转依赖导航策略而非横幅隐藏。未填写真实账号凭据。
- 本机无法代替已安装 Grok App 的真机完整验证；Universal Link 不再唤起 App、用户真实授权回跳及新会话确认仍需设备复测。未提交 / 推送 / 上传。

### 2026-09-05 · 0.4.543 · 修复截图中的六条编译告警

- 三个独立根因，z +3：`DashboardSceneMath.lerp` 移到公开类型主体，保持公开 API 和其余辅助方法私有；SSO 域名判断用 `contains(where:)` 消除 `if` 尾随闭包歧义；离屏导航策略移除 `nonisolated`，继承 `WebViewFetcher` 的 `@MainActor`，与当前 SDK 的 `WKNavigationDelegate` 隔离一致。导航拦截条件及 decisionHandler 调用路径不变。
- `xcodegen generate` 成功；Core 全量 **1,079 项、0 失败**，含全部 `LoginConfirmTests`；`UsageLimits` Debug App scheme 构建成功。新日志 `/tmp/usagelimits-warning-tests.log`、`/tmp/usagelimits-warning-build.log` 中截图的访问级别 / 闭包 / 四条 WebKit 隔离告警均消失。构建仍有工具提示 `Metadata extraction skipped. No AppIntents.framework dependency found.`，不是本次 Swift 源码告警。
- 本轮只验证编译和回归测试，未执行真机第三方登录；未提交 / 推送 / 上传。

### 2026-09-05 · 0.4.540 · 登录检测与新增供应商 / 积分明细体验

- Grok / Cursor 登录检测改为检查当前可见的官网页面（含 OAuth 弹窗），使用新探针结果，不能用 last-good 缓存或 Grok 游客额度弹出登录成功。保留 WebKit 提供的弹窗 configuration / opener，补齐各自官网跨域回跳；Cursor 离屏探针入口改为 Dashboard。拒绝一次确认后，重新进入明确登录页会重置确认状态，普通刷新不会反复弹窗。
- 用户日志中的 Grok 403 含 `WKE=unauthorized:second-factor-needed`；单独提示浏览器验证，并保留已有有效额度，不能将其一概解释为 Cookie 失效。Cursor 日志是 `auth_me` 204 无身份、用量接口 401，没有证据证明站点已把会话交给当前 WebView。两家目录和脱敏回归夹具同步更新；本轮未实现或绕过 Grok WKE，也未据此宣称真实账号登录恢复。
- 新增供应商的名称框改为带编辑图标、柔和圆角背景与聚焦描边的输入区；有内容时显示清空按钮，默认名称和添加行为不变。清空按钮补齐五种语言。独立模拟器验证浅色 / 深色、输入、聚焦与清空。
- 即梦近一个月明细统一限制五条可见高度，不足五条按实际条数收缩；超出记录保留在内部滚动区，展开卡片也不再平铺全部历史。行高随动态字体缩放，明细区域单独登记场景手势排除范围。模拟器平铺主题已从第 1–5 条滚至第 2–6 条，卡片高度和相邻卡片位置保持不变。
- Core 全量 **1,079 项、0 失败**（含 `LoginConfirmTests`）；`xcodegen generate` 和 `UsageLimits` Debug App scheme 构建通过。日志为 `/tmp/usagelimits-final-tests.log`、`/tmp/usagelimits-final-build.log`。本轮四项改动从 0.4.536 累计增加 z 至 **0.4.540**，未提交 / 推送 / 上传。
- 设备验证边界：轮盘主题五条明细布局已查看，但当前模拟器工具未完成其内部手势验证；临时放开父滚动的对照也无效，诊断改动已撤回。轮盘 / 螺旋内部拖动、真实 Grok 验证与 Cursor OAuth 会话仍待真机复测。模拟器 Grok 停在登录页时未误报成功，手动检查会提示完成官网登录；这不等于第三方真实登录闭环。

### 2026-09-05 · 0.4.536 · UsageLimits 内部命名迁移

- 工程 / 唯一 scheme / 主产品统一为 `UsageLimits`，核心模块和目录统一为 `UsageLimitsCore`，Widget / Watch、入口类型、import、源码契约路径、entitlements 文件名、脚本和当前开发文档同步迁移。旧轮次以下保留当时工程名，不作为现行构建命令。
- 新生成深链采用 `usagelimits://`，继续接受 `aiusage://`。Bundle ID、App Group、后台任务 ID、签名团队、存储键、账号 UUID、身份哈希盐、Widget kind 和已有 AppIntent 类型 / 参数不变。新深链计 z +1，x / y 不变；本轮未提交、未上传。
- XcodeGen 只定义共享 scheme 仍会被 Xcode 自动补出依赖目标的 scheme；新增 `postGenCommand` 调用 Swift 脚本，保留其它 workspace 设置并关闭自动创建。实际 `xcodebuild -list -json` 只剩 `UsageLimits`，不手改 pbxproj。
- 全量 Core 1,069 项、0 失败；Debug / Release 完整构建通过，App / Widget / Watch 产品、Bundle ID、版本字段、双 scheme 及签名配置核验通过。新旧 AppIntent 元数据的差异仅模块 / 编译 / 查询限定名，稳定数据标识一致。
- 独立模拟器旧版直接覆盖安装 Debug / Release：安装前后和启动后 34 个关键存储项一致，新 Core 可解码 27 个账号 / 快照，自定义 token 哨兵、模板、logo、身份指纹保留；429 个 WebKit 文件（含 289 个附加账号 UUID 存储文件）在安装后首次启动前均哈希一致。未执行真实账号登录，不能据此推断真实会话有效。
- 模拟器启动、设置、分享预览、小组件预览、新旧账号 / 供应商深链已实测。旧 Widget 的账号及计量配置在系统编辑器保留，但桌面旧时间线刷新未闭环。旧快捷指令参数仍存在，新旧构建均再次询问主题，选择后均可切换；不计为无提示参数恢复验证。
- 详细证据、命令日志位置、旧名例外和真机 / 分发限制见 [命名迁移验证](docs/plans/2026-09-05-usagelimits-validation.md)。

### 2026-09-05 · 0.4.535 · 发布轮次调整

- 按用户明确指令将 y 改为 4，版本由 `0.2.535` 调整为 `0.4.535`，x / z 保持不变。版本仅在 `project.yml` 维护，随后重新生成工程；本轮同时提交分享明细间距与展开卡片滑动修复。

### 2026-09-05 · 0.2.535 · 分享明细间距与展开卡片滑动

- 分享明细：无进度条的指标行原高 20pt，却沿用进度条行的 `ly - 8` 明细位置，标签与明细重叠。无条指标现在取消上移，并在测量及绘制中同步增加 8pt 占高；有条指标保持原布局。新增真实位图回归测试，修复前文字只有一条连通墨迹带，修复后标签与明细分离且至少有 3pt 可见空白。用现有智谱 / MiniMax fixture 生成并检查了[明细分享图](docs/screenshots/0.2.535/share-zhipu-minimax-details.png)。未改探针、Parser 或供应商口径。
- 展开卡片：原来展开时关闭场景识别器，必须点按收起才能滑动。现在展开态继续识别手势，拖动开始在同一手势中收起并交给场景处理；菜单按钮仍排除场景手势，只有实际溢出的长卡内容区域接管内部滚动。移除重复原生 Tap 手势，卡片 / 空白点击收起统一由场景路由处理。
- 快速滑动：越过阈值的首个 move 原先只发 begin，丢失已移动距离；只有 began / ended 而没有 moved 时还会误判为 tap。现在按顺序补齐 begin → change → end，只交付实际起终点位移，无中间采样时不推测惯性。禁用 / 取消 / 重置均清空待交付事件。
- 验证：新增测试先失败再修复；最终完整 `swift test --package-path Core --disable-sandbox` 1,066 项、0 失败；`xcodegen generate` 与 `AIUsage` Debug 完整 scheme 构建通过（Xcode 26.6、iPhone 17 / iOS 26.5），构建产物版本 `0.2.535`。`git diff --check` 通过，无临时诊断代码。此版未重复 Release 构建，前述 `0.2.533` 的 Release 结果不充当此版结果。
- 模拟器实测：旧版本 Claude 展开后拖动无效；新版本轮盘中展开 Claude 后一次上滑切到 ChatGPT，螺旋中一次上滑切到 Grok；展开后普通点按只收起一次；DeepSeek 展开态菜单正常打开 / 关闭。临时日志确认同一手势完整交付 begin / change / end，随后已移除日志。长卡的大字号溢出滚动测试因用户接管模拟器而停止，字号已恢复原值 `large`；该分支目前有源码契约覆盖，但不算完成设备实测。
- 真机待验证：连续慢拖 / 快滑、长卡内部滚动与从标题区域切换、长按计量条菜单、双指排序、VoiceOver。两项独立修复使 z +2，`0.2.533` → `0.2.535`；未提交。



（每轮：build → install → launch → screenshot → log 扫描；结果与截图结论记在这里）

- **R0 逻辑层**：Core 包 `swift test` 18/18 通过（三家解析器 fixture、防御式容错、存储 round-trip、诊断环形缓冲）。
- **R1 冷启动（暗色）**：BUILD SUCCEEDED（App+Widget 首轮编译零错误）；`AIUsageWidget.appex` 正确嵌入 PlugIns；launch 无崩溃；主界面三卡片渲染正确（未登录态、品牌色按钮、隐私声明）。截图 `r1-dashboard.png`。
- **R2 演示模式**：`--demo` 启动，三卡片展示套餐徽章（Claude Max 5x / ChatGPT Plus / SuperGrok）、进度条阈值配色（61% 橙）、重置倒计时中文文案、订阅到期、剩余次数（剩 18/20）全部正确。截图 `r2-demo.png`。
- **R3 小组件预览**：2×2 环形（34% 已用 + 窗口名 + 重置时间）、2×4 三行总览（进度条+标签+重置+百分比）布局无溢出。截图 `r3-widgets.png`。
- **R4 Claude 登录页**：WKWebView 加载 claude.ai，**Cloudflare 检查显示「验证成功」**——Safari UA 的 WebView 方案通过防护验证可行（裸 URLSession 会被拦）。截图 `r4-login-claude.png`。
- **R5～R7 端到端管线（真实网络，共 7 轮探针）**：`--auto-refresh` 启动即刷新三家 → 读模拟器 App Group plist 验证落盘。结果：Claude 正确判「未登录」；ChatGPT 检出**用户真实会话**（代理注入）判「已登录」，套餐/额度端点 401 如实显示「官方未提供数值额度」；Grok 游客额度 3 条指标 + 「游客额度」徽章 + 登录按钮。期间发现并修复 #5/#6/#7/#9。截图 `r7-real-data.png`。
- **R8 浅色模式**：演示模式主界面与小组件预览在 light 外观下渲染正常。截图 `r8-light-demo.png`、`r8-light-widgets.png`。
- **R9 登录自动检测闭环（真实账号）**：`--open-login openai` 打开 ChatGPT 登录页 → 2 秒后探测到已登录 → 状态提示 → **自动关闭回主界面**，全流程无人工干预验证通过。截图 `r9-login-openai.png`（sheet 已自动关闭后的真实数据主界面）。
- **R10 Grok 登录页**：显示 "This service is not available in your region."——本机网络对 grok.com 页面的区域分流问题（API 探针反而可用），属环境边界非 App 缺陷。截图 `r10-login-grok.png`。
- **R11 桌面图标 + 日志扫描**：程序化生成的三环仪表图标正确显示在主屏（截图 `r11-home-simulator.png`）；`log show` 扫描 30 分钟内 AIUsage 进程仅系统级噪音（launch measurement、WebKit 统计等），**零崩溃、零 App 级错误**，DiagnosticReports 无 AIUsage 崩溃报告。
- **终态**：Core `swift test` 33/33 绿；`xcodebuild build`（App+Widget）零错误零警告；`AIUsageWidget.appex` 嵌入正常。 Grok 展示已按官网 Settings → Usage 校准（见 #12）。
- **R12 目录/开关/折叠/标价 四合一（2026-08-16 ultragoal）**：新增 `providers/` 接口目录、设置页服务商开关（App Group 持久化）、首页折叠卡片（默认折叠，展开显示全部有用量指标，0% 指标默认隐藏可按需显示）、卡片抬头「商标+套餐+官方月标价」（`PlanCatalog`，如 SuperGrok Heavy $300）。验证：Core `swift test` 40/40 绿；build 首轮零错误；模拟器五轮截图——折叠态（`g3-collapsed.png`）、展开态（`--expand-cards`，`g3-expanded.png`）、0% 指标隐藏 + 「显示 1 项未使用的指标」（`g3-unused-hidden.png`）、关闭 Grok 后首页/设置页/小组件预览均不再出现（`g2-grok-disabled.png`、`g2-settings.png`、`g2-widgets.png`）、重新开启即恢复（`g2-grok-restored.png`）；`log show` 扫描仅系统级噪音，零崩溃零 App 错误。本轮无新问题点。

- **R13 动画/主题/小组件五合一（2026-08-16 /goal）**：Core `swift test` 59/59 绿；build 两轮零错误。模拟器截图验证：折叠态四卡默认显示最长窗口指标（Claude「All models」61%、ChatGPT「Codex 7 天窗口」、Grok「本周限额」、Cursor「Cursor Models」，`v1-collapsed.png`）；展开态套餐+价格徽章紧凑行、未使用指标折叠、更新时间（`v2-expanded.png`）；设置页服务商折叠头行（4/4 + chevron）与「主题」分段选择器（`v3-settings.png`）；共享容器写入 `appTheme=light` 后深色系统下 App 全浅色渲染（`v5-light-theme.png`，同时证明 App 读写已走 App Group 容器——`simctl get_app_container … groups` 确认容器已注册，#22 的同步机制在模拟器打通）；2×4 单服务商（3 行）与 2×4 总览（4 行）行均匀铺满整卡无空白（`v6-widgets.png`，4×4 与 2×4 共用同一布局代码）。折叠/展开与拖动动画属交互过程，静态截图不可证，待真机手感复验。#22 真机重装验证仍待人工。

- **R14 Apple Watch 应用（2026-08-16 /goal）**：Core `swift test` 59/59 绿；`xcodegen generate` 后 iOS+Widget+Watch 三目标一轮构建零错误（entitlements 经 properties 重生成仍含 App Group）。Apple Watch Ultra 3 模拟器截图：Claude 圆环页（三环 + Current session/All models/Fable 图例 + 横向页点，`w1-rings.png`）；`--provider grok` 直达 Grok 页（`w2-grok.png`）；`--open-settings` 直达设置页开关列表（`w3-settings.png`）；**端到端同步**——手表全新安装、不带 `--demo` 启动，配对 iPhone 17 模拟器上的 App 开演示模式后，手表经 WCSession 收到完整状态并渲染圆环（`w7-synced4.png`，装表 App→重启手表→先表后机启动的顺序见 #29）。表端开关回传 iPhone 与真机配对安装待人工验证。

- **R15 提醒系统（2026-08-16 手机端提醒需求）**：Core `swift test` 最终 74/74 绿（新增 15 条：设置持久化/去重键环形上限、阈值穿越+去重+窗口重置后重新武装、旧快照 needsLogin/指标瞬时缺失不算穿越、相对 resetsAt 漂移被小时桶吸收、回满检测只认最长窗口且 session 不触发、resetDedupeKey helper 与事件键一致、`planExpiresAt` 老 JSON 兼容、OpenAI 解析回填到期时间）；`xcodegen generate` 后 `xcodebuild build` 零错误零警告（App 目标；Watch 目标存在 3 条既有 Sendable 警告，非本轮引入）。**独立 code-review 一轮**：4 条 HIGH 全修（前台展示需 `UNUserNotificationCenterDelegate`、去重键改为投递确认成功后才登记、BGTask 过期收尾必须 `setTaskCompleted`、相对 resetsAt 去重键漂移→小时分桶），另修 6 条 MEDIUM/LOW（时间触发器替代日历触发器防时区/夏令时漂移、到期按日历天回推、开关/退出登录联动撤销预排提醒、后台刷新结果回灌 UI、同一次重置只发一条、重排防抖、空 Section 空行、百分比四舍五入）；`longestWindowMetric` 在周重置前约 5h 可能被 session 反超记为已知边界。模拟器截图：提醒二级页关闭态（`/tmp/notif-settings.png`）、经 App Group 容器写入设置后开启态阈值滑杆 85% + 提前 5 天 Stepper（`/tmp/notif-settings-on.png`）、设置一级页「提醒 ›」入口（`/tmp/settings-entry.png`）、审查修复后终版（`/tmp/notif-final3.png`）；`log show` 仅系统级噪音，零崩溃零 App 错误。新调试参数 `--open-notification-settings`。通知实际到达（权限弹窗、锁屏转发手表、BGAppRefresh 触发频率与后台探针成功率、wham 报文是否走相对秒数分支）依赖真机与真实账号，待人工验证。

- **R16 OpenCode 接入（2026-08-26，0.2.165）**：新增第 10 家内置供应商 `opencode`（Zen 余额 + Go 三窗口同一张卡）。**问题**：opencode.ai 控制台没有 REST，全部是 SolidStart server function（`POST /_server` + seroval 序列化/流式反序列化），且 server id 是 64 位 hex、chunk 名带构建哈希。**处理**：探针不自己拼 seroval，而是在同源里 `import()` 页面自带的 `server-runtime-*.js`，用 `createServerReference(id)(workspaceID)` 拿反序列化对象；chunk 名从 `/workspace/<id>` SSR HTML 正则、`billing.get` / `lite.subscription.get` 的 id 分别从 `common-*.js` / go 路由 chunk 正则自校准，失败再回落写死 id。三条正则已用线上真实 chunk 经 node 验证命中；`node --check` 语法通过。登录判定走无鉴权的 `GET /auth/status`；落点不在 `opencode.ai`（停在 auth.opencode.ai / GitHub）直接判未登录。Core `swift test` 359/359 绿（新增 8 条：Go+Zen 混排与排序、月上限只算当月、Black 隐藏 Go、`mine=false`、未登录、runtime 缺失是 error 不是登出、脏报文不崩、脚本自校准断言）；`xcodebuild build` 零错误。**待人工**：`_server` 响应字段名来自 bundle 读取代码，尚未在真机登录态抓包核对（`providers/opencode.md` 已标注）；GitHub / Google OAuth 弹窗链路与 `createWebViewWith` 共用，需真机点一次。

- **#R16-1 OpenCode 登录成功但检测不到登录（2026-08-26 真机，0.2.166）**：**现象**：内置页 GitHub 登录成功停在 `/workspace/<wrk_id>`，App 仍提示需登录。**原因**：登录前的静默探测把离屏 WebView 从 `/auth` 302 带到 `auth.opencode.ai`（OAuth 页）；`WebViewFetcher.hostMatches` 是后缀匹配，`auth.opencode.ai` 命中 `.opencode.ai`，`ensureLoaded` 视为「已在源上」永不重载，登录后探针一直在 OAuth 页上跑，脚本 `__ocOnSite()` 为 false → 永远 `needsLogin`（Kimi 的 `auth.kimi.com` 与 `www.kimi.com` 不同主域，所以早先没踩到）。**处理**：新增纯函数 `OpenCodeSession.isProbeReady(url:)`（主机精确 `opencode.ai` 且路径 `/workspace/wrk_…`），`ensureLoaded` 对 OpenCode 只在就绪时复用，否则重载 `/auth`；`OpenCodeSessionTests` 4 条锁住判定与接线。**状态**：已真机复验通过——`devicectl` 装 0.2.166 到 iPhone 17 Pro 并 `--auto-refresh`，从 App Group 容器拉诊断：旧版行 `opencode.billing: off-site: auth.opencode.ai → needsLogin` 印证根因；新版 `opencode.status` 200 含 account、`billing` 200、`parsed= status=ok`（Zen 余额 / 月上限），`lite` 为 `null`（未订 Go）。顺手：`DiagnosticRedactor` 增加 `customer` / `payment` 键片段脱敏，fixture 与目录文件按真实报文校准。

## 无法全自动闭环的已知边界（人工步骤）

1. **真实登录三家并看到订阅用量**：需要用户账号凭据，无法自动化。已用真实 ChatGPT 会话验证「已登录」链路；Claude/Grok 登录后链路与其共用同一管线 + 防御解析。
2. **把小组件添加到桌面**：iOS 无公开自动化 API（springboard 不可脚本控制）。手动：桌面长按 → 「+」→ 搜「AI 用量」→ 选 2×2 或 2×4。App 内「设置 → 小组件预览」已用同一套视图代码在两种真实尺寸下截图验证。
3. Grok 登录页的区域限制、ChatGPT wham/accounts 端点 401：网络环境/端点漂移类问题，诊断日志内可持续观察。
| 51 | 用户要求 | TF「App 信息」邮箱装新版后仍是旧值；且不希望 App 内出现反馈邮箱/链接 | 该邮箱与商店「开发者」名称存在 App Store Connect 里，不在 ipa 内，上传新构建不会改变 | 删除设置页「反馈邮箱」行、`AppContact`、`settings.feedbackEmail` 本地化键；CLAUDE.md 隐私节改为「App 内不放联系方式」。ASC 反馈邮箱 / 支持 URL 需在网页改（0.2.167） | ✅ |
| 52 | 真机实测 | 首页长按任意卡片立即崩溃，无明显日志 | `DragSessionItemProvider` 继承 `NSItemProvider` 并只定义了 `init(object:onEnd:)`；`-[NSItemProvider initWithObject:]` 内部会再调 `[self init]`，子类没有 `init()` → Swift fatal「Use of unimplemented initializer 'init()'」（Xcode 断在 DashboardView.swift:477） | 不再子类化：用普通 `NSItemProvider(object:)`，通过 `objc_setAssociatedObject` 挂一个 `DragSessionToken`，随 provider 释放时 deinit 清拖动态，#42 的行为不变；临时 XCUITest（长按原地松手 / 拖到另一张卡 / 拖到空白）验证不崩 | ✅ |
| 53 | 真机实测 | 「编辑计量顺序」拖完后，下一次刷新用量顺序就回到解析器默认 | 顺序只在 `SharedStore.snapshot(for:)` / `accountSnapshot(for:)` 读出时套用；`AppState.refresh*` 落盘后直接把解析结果塞进内存 `snapshots` / `accountSnapshots`，没有回读 | 三处提交点落盘后改为回读 store（`committed = store.snapshot(for:) ?? snap`），内存态与落盘态同源 | ✅ |
| 54 | 真机截图 0.2.367 | 2×2/2×4/4×4 系统编辑页「额度」全是空「选取」槽 | ① 整数字面 `size: N` 编成 `IntentCollectionSize(exactly: N)`，系统画 N 个空槽；② AppIntents 只从 query `defaultResult()` 取默认，且 `DefaultValue` 须是 `[Entity]`，旧 query 返回单值/nil，`size` 不会预填 | 按尺寸拆 Intent；`@Parameter(..., size: IntentCollectionSize(min: 1, max: N), query:)`（iOS 18）；各 query `typealias DefaultValue = [Entity]` + 数组 `defaultResult`；总览账号丢掉 `auto.highest`，4×4 上限 4 | ✅ |
| 55 | 真机截图 0.2.367 | 首页向下拖相邻卡永远少一格、落点抖 | `AccountCardDropDelegate.dropEntered` 一碰目标任意像素就按「插到目标前」提交，无中线；换位后布局在指下重排再触发 enter/exit。`applyOrder` 每次换位同步 `WatchSync.pushState()` 全量 JSON 掉帧。设置页 `.onMove` 长按与 `.contextMenu` 抢手势 | 容器级 `AccountListDropDelegate` + `DragReorder.decision`（中线 ±6pt 滞回）；Watch 推送 0.6s 合并；设置页「排序」切 `editMode` 出系统拖柄 | ✅ |
| 56 | 真机 0.2.375 | 所有小组件编辑页「无法载入」 | ① 把 `SingleProviderConfigIntent` / `OverviewConfigIntent` 改名，桌面旧实例反序列化失败；② query `typealias DefaultValue = [Entity]` 与 EntityQuery 默认 `DefaultValue = Entity` 冲突，系统加载任一编辑页都会崩 | 恢复旧 Intent 名并继续给 2×2/2×4、2×4/4×4 共用；预填改 `@Parameter(default:)` + `IntentCollectionSize(min:max:)`；`defaultResult` 回到单个 Entity | ✅ |
| 57 | 真机截图 0.2.376 | Claude 首页 3 条额度，选取额度不预填 | `@Parameter(default:)` 抽 metadata 时 SharedStore 为空；又写死 `limit: 2`，且 `all().first` 是最高用量自动 | 稳定 `home.slot.N` 占位 + `WidgetEditorPrefill` 映射首页 `activeMetrics`（含改序）；2×4 最多 4 可增删；4×4 每家最多 2 | ✅ |
| 58 | 真机截图 0.2.377 | 已选额度无法删除（无红色减号） | query `entities(for:)` 把 `home.slot.N` 换成真实额度 id，系统按 id 删除对不上；`IntentCollectionSize(min: 1)` 还锁死最后一条 | 编辑页改为 `editorItems` 保留槽位 id；额度集合 `min: 0`；时间线空配置仍回落首页 | ✅ |
| 59 | 真机截图 0.2.378 | 多账号总览已选额度全叫「额度」，不按实例分组 | 占位实体 title 写死「额度」且 `displayRepresentation` 在 accountTitle 空时只显示 title；`displayedAccounts` 按首页顺序不是编辑页实例顺序 | `resolvedDisplay` 把槽位换成真实额度名；已选行 title=额度名、subtitle=实例名；添加列表 `IntentItemSection` 按实例分组；组顺序跟 `orderedAccountIDs` | ✅ |
| 60 | 真机截图 0.2.379 | 多账号总览已选额度仍全叫「额度」，分组未生效 | 系统已选列表不画 `IntentItemSection` / subtitle；把 query `Result` 改成 `IntentItemCollection` 后已选行回落类型名「额度」 | 撤回 Result 分组；title 写成 `实例名：额度名`；按上面实例顺序排序；空账号时用首页实例解析名称 | ✅ |
| 61 | 真机截图 0.2.380 | 多账号总览已选额度是「Grok-main：本周限额」扁平行，不是实例分组 | 系统已选列表不画 IntentItemSection；上一版只能把实例名写进同一行 title | 预填插入 `home.header` 组头行，title=`实例名:`，下面额度只显示额度名；组顺序跟上面实例；时间线丢掉组头；旧实例无组头时第一行用换行兜底 | ✅ |
| 62 | 真机截图 0.2.381 | 选取额度变成 Grok-main：/本周限额/Grok Build/Claude：，Claude 额度消失 | 组头实体占已选槽；系统列表不能多塞行，前 4 项变成组头+两条+空组头 | 预填只放额度槽；每组第一条 title=`实例名:\n额度名`；381 留下的空组头补回该实例第一条额度名；组头不进时间线 | ✅ |
| 63 | 用户要求 0.2.383 | 系统已选列表画不出组头，再改 title / 塞 header 都会失败 | 总览额度参数是扁平 `[Entity]`，没有分组 API | 总览去掉选取额度；2×4 只 1 实例取首页前 4 条；4×4 按实例数 8/4/3/2；组顺序=编辑页实例顺序（0.2.383） | ✅ |
| 64 | 用户截图 0.2.383 | 设置「用量展示」脚注两句太长；预览 2×4 单账号和总览重复 | 2×4 总览已改成单实例，预览再分两种没有场景 | 脚注收成一行「提醒阈值始终按已用计算。」；预览只留一块 2×4，标题不再写单账号/总览（0.2.385） | ✅ |
| 65 | 仓库自查 | 即梦抓包、设备日志、Xcode 中间产物进了 git | `.gitignore` 写成 `logs/my-interface-docs/` 一条，既忽略不了 `logs/` 也忽略不了根目录抓包；`Core/build/` 从首提交就跟踪 | 移出抓包与日志；忽略 `logs/`、`my-interface-docs/`、`Core/build/`、`HANDOFF.md`；历史提交去掉这些路径 | ✅ |
| 73 | 真机 0.2.474 螺旋 | 横向拖到头后链条断开，画面下半和右侧出现大片空白 | 参考稿倾斜范围 ±70°，超过线圈段尽头（`twistCapDegrees / twistGain` = 43.75°）后进入 flatten 段把链条拉平铺开；参考 390×844 画布上勉强能看，手机竖屏上就是断链 | 新增 `DashboardSceneMath.helixCoilTiltRange`（±43.75°），`setTilt` 与初始倾斜都夹在线圈段内，不再进入 flatten；`helixTiltRange` 仍给数学层用 | ✅ |
| 74 | 真机 0.2.474 螺旋 | 屏幕左右边缘各一条竖线，像一圈线框 | `DashboardSceneChrome` 的两条 1pt 导轨 Capsule（参考稿装饰），在真机白底上被看成线框 | 删掉导轨，只留紧凑光晕 | ✅ |
| 75 | 真机 0.2.474 轮盘 / 螺旋 | 转动没有声音，用户要金属齿轮咔哒声 | 主题 SDD 当初约束「只触感、无音频」，参考稿其实带 tick.wav（轮盘）和合成齿轮声（螺旋） | 把参考 `tick.wav` 打进 App 资源（`App/Resources/SceneTick.wav`），`DashboardSceneTick` 走 `AudioServicesPlaySystemSound`（静音拨片下不响、两声至少隔 36ms）；换卡一声，螺旋横向每 3.2° 一声，与触感同步 | ✅ |
| 76 | 真机 0.2.481 iPhone 16 | 相机控制键完全不生效：滑动没反应，按住直接跳系统相机 | 相机会话只挂了输入、没有任何输出，`startRunning` 后没有连接、设备不真正开流，系统不把 App 当「正在使用相机」，硬件键仍走系统默认；事件宿主也只是个 0×0 的 UIView | 挂一个 `AVCaptureVideoDataOutput`（帧全部丢弃、开流后把帧率压到格式最低），宿主换成铺满首页、`hitTest` 返回 nil 的穿透视图；会话启动结果与权限写进探针诊断日志（`sideKey:` 前缀）并打到控制台；已用 `devicectl device process launch --console … --camera-side-key` 在 iPhone 17 Pro 上确认 `camera session running, controls 2`，硬件键按下的链路待用户手按 | ✅ 会话已验 |
| 77 | 真机 0.2.481 螺旋 | 卡片像一叠错开的卡，看不出螺旋；用户要更大的圆弧半径、更多卡片 | 参考稿靠 CSS `perspective: 980px` 把 z 投影成大小，移植时只保留了旋转透视，后方卡片不随深度缩小；碰撞下限又按未投影整卡算（App 卡尺寸下 187pt），一屏只剩 4 张 | `helixPose` 把 z 折进 scale、x 也按投影收拢；碰撞下限放宽 0.6 倍、默认螺距按卡高等比；新增线圈半径增益（默认 1.35，横向拖动 ±，`resetHelix` 回默认）让 x 摆幅和 z 深度一起放大 | ✅ |
| 78 | 真机 0.2.481 螺旋 | 屏幕角落斜着一拖，卡片间距被拉得很稀 | 参考稿的斜向手势 = 调螺距，用户当 bug | 斜向归到占优的轴（竖 = 转盘，横 = 半径），螺距不再有手势；真机 `--helix-corner-drag` 回放 45° 斜拖：spacing 112.31 → 112.31、position 0 → 4.0 | ✅ 真机已验 |
| 79 | 真机 0.2.484 iPhone 17 Pro | 开着相机控制键开关启动，App 偶发直接崩溃 | `configureAndRun` 用 `defer { commitConfiguration() }`，而 `startRunning` 派发到会话队列，可能先于 commit 执行：`startRunning may not be called between calls to beginConfiguration and commitConfiguration`（NSGenericException）。之前几次真机启动只是碰巧没撞上 | 每条早退路径显式 commit，正常路径在派发 `startRunning` 之前 commit；`--dump-screenshot` 连续两次真机启动无崩溃、`frames received 6 in 3s` | ✅ 真机已验 |
| 80 | 真机 0.2.484 螺旋 | 横向拖远后方卡片：卡片变小了但螺距不变，链条断成几段 | `helixPose` 只把投影用在 x 和 scale，y（螺距）没投影 | y 也乘同一投影：越远越密，链条连续；线圈增益上限从 2.6 收到 2.0（2.4 时远端卡片全躲到前排卡片背后）。真机默认态截图 8 张连续圆弧 | ✅ 真机已验 |
| 81 | 真机 0.2.486 iPhone 17 Pro | 用相机控制键确认「设置」后，页面卡好几秒才响应 | 进设置页时首页停相机会话、回来时重建，这两步（`setControlsDelegate(nil)`、`stopRunning`、`addInput/addOutput/commitConfiguration`、`startRunning`）都在主线程做；浮层还激活着时 `setControlsDelegate(nil)` 尤其慢 | 会话的建 / 拆 / 换控件全部挪到 `sessionQueue`，主线程只摘引用改状态 | ✅ 代码已改，待真机复验 |
| 82 | 真机 0.2.486 | 相机键的镜像菜单竖直居中，和机身右侧的相机控制键对不上 | 菜单用 `frame(alignment: .trailing)` 居中 | 按整屏高度 66% 定位（`cameraControlScreenRatio`），换算到覆盖层坐标，上下各留 80pt | ✅ |
| 83 | 真机 0.2.486 | 轻按弹出的浮层有时是「滚动页面」滑杆：一滑卡片就动、镜像菜单被收掉，进不了菜单；滑杆 0…1 有长度，轮盘 / 螺旋没法一直循环翻 | 会话里同时挂了菜单选择器和滚动滑杆，系统浮层显示哪只不由 App 决定；滑杆天然有界 | 同一时刻只挂一只选择器：空闲挂「上一张 / 翻页 / 下一张」选择器（选完自动回中，无限循环），按下才换成菜单选择器，主题项换主题选择器，确认后换回翻页；浮层出现 / 消失不再改菜单状态 | ✅ 代码已改，待真机复验 |
| 84 | 真机 0.2.489 螺旋 | 看起来只有一层螺旋，上下大片空白；用户要一些虚化的前景卡片 | 参考稿几何 `z = D(cosθ − 1)`：焦点卡是线圈最前点，其余全在后方 | 「前景抬升」`helixForegroundLift = 0.9`：线圈整体向观者推进 0.9 × 深度、焦点卡附近平滑扣回（slot 0 仍 z = 0），前后两张邻卡到观者前方（更大、按靠近程度虚化 ≤ 6pt、略透明），再远的绕到后面。相位法（整条线圈绕轴转）试过：卡片全甩出屏幕，弃用 | ✅ 模拟器验，待真机 |
| 85 | 真机 0.2.489 | 从设置页返回，侧边键菜单总是被强制弹出 | 用 `--open-side-key` 启动过的进程一直活着，首页每次重新出现 `onAppear` 都再弹一次 | 只在首页第一次出现时弹（`didAutoOpen`） | ✅ |
| 86 | 真机 0.2.491 螺旋 | 前景虚化卡压住了主卡片的标题；用户裁定：前景虚化无论如何不能挡主卡 | 层叠按 z 排，前景卡 z > 0 自然压在焦点卡上面 | 焦点卡 `zIndex` 固定 5000（展开态 10000 更高），前景卡永远在它之下 | ✅ |
| 87 | 真机 0.2.491 螺旋 | 用户要真实螺旋：线圈对面的卡片应显示反转（背面）的内容 | 卡片 `rotationY` 只在 ±7° 内摆，全部正对观者 | 卡片贴线圈切线朝向轴心：`rotationY = θ`（折到 ±180°），对面 \|θ\| > 90° 由透视渲染自然露出镜像的背面；焦点卡 θ = 0 正对。竖拉变长（螺距）受首页手势限制暂未做（竖向是转盘） | ✅ 模拟器验 |
| 89 | 真机 0.2.494 iPhone 17 Pro | 首次打开 + 每次切换主题必卡顿，与刷新无关；诊断日志里看不出主线程在做什么 | 待定位：`setActive` 已是 0 ms，快照在 AppState 缓存，存储不在热路径；剩下的嫌疑是首页 9 张 3D 卡片首帧栅格化（阴影 / 虚化离屏）或某处同步等待，猜不出来 | 加 `MainThreadHangWatchdog`：主线程 ≥300 ms 无响应就 SIGPROF 抓栈写进诊断日志（`hang:` 前缀，镜像+偏移），用归档 dSYM `atos` 符号化定位。真机日志三次 `hang: 8861 / 9154 / 8972 ms`，栈顶 `AIUsage+0xa7e48` = `DashboardSideKeyController.start()` 里 `session.setControlsDelegate(self, queue: .main)`：主线程同步等相机服务 XPC 9 秒，每次首页出现（首次打开、从外观页回来）都触发 | ✅ 已定位（见 #90） |
| 90 | 真机 0.2.495 | 同 #89；用户裁定：不论是否相关，App 不要调用相机（隐私） | 相机控制键的硬件事件只发给正在用相机的 App，方案本身要求常驻相机会话 | 整体拆除相机会话（AVFoundation / AVKit、`NSCameraUsageDescription`、外观页开关）；侧边键改为操作按钮 + 快捷指令（App Intents：打开侧边菜单 / 切换首页主题 / 分享用量 / 打开设置），屏幕菜单挪到左缘对齐操作按钮；卡顿随会话一起消失 | ✅ |
| 91 | 真机 0.2.501 Grok | 登录、登录检测都成功，卡片却不显示任何用量 | `weekly`（gRPC-Web protobuf）本周还没用时报文只剩 currentPeriod：proto3 不上线默认值 0，解析器把「没出现」当「未知」返回 nil，指标被 `hasUsage` 隐藏；解析成功但快照无数字又被 `RefreshPolicy` 当不可靠拦下 | protobuf 路径有周期没百分比即 0%（`percentIsWirePublished` 仍记 false）；周限额指标 `pinned` 常显，0% 也画 | ✅ |
| 92 | 真机 0.2.501 Gemini | `quota` HTTP 200、恰好 200000 字节，解析报「配额数据异常」 | 探针文本上限 `__PROBE_TEXT_LIMIT_BYTES = 200000`，Gemini quota 报文超过 200 KB 被截断成坏 JSON。排查其它供应商：MiniMax usage_summary 87 KB、DeepSeek usage_periods 94 KB 尚在上限内，只有 Gemini 中招 | 上限提到 1 MB（二进制上限不变） | ✅ |
| 93 | 真机 0.2.501 场景主题 | 手指按在计量条上滑不动转盘 | 计量条为了长按弹「编辑计量顺序」被登记成场景手势的排除区域，触摸从这里开始整个场景手势就失效 | 新增「延后判定」区域：触摸可从计量条开始、短触 / 拖动照常滚动，只是不从这里起整卡排序的长按；长按仍由计量条的上下文菜单接管 | ✅ |
| 94 | 真机 0.2.501 反馈 | 选「携带日志」有时邮件里没有附件 | 附件以内存 `Data` 在 `makeUIViewController` 一次性挂上，遇上弹窗切换时偶发丢失 | 先把日志导出成文件（临时目录固定文件名），附件从文件读，`updateUIViewController` 里未挂上就补挂一次 → 真机 0.2.513 仍无附件。第二次：撰写器不再走 SwiftUI `.sheet` + `UIViewControllerRepresentable`（MFMailComposeViewController 被当子控制器嵌入承载控制器，远程撰写界面起来后附件丢），改为 UIKit 从最上层控制器直接 `present`，附件在 present 前挂好；挂载结果写进诊断日志（`feedback: attached … bytes`） | 🔁 0.2.525 待真机复验 |
| 95 | 真机反馈 侧边键 | 「侧边键按钮点击无效」 | 未复现。模拟器 iOS 26.5：屏幕菜单在平铺 / 轮盘 / 螺旋点「设置」「主题 → 螺旋」（含按坐标点）都生效；通过 快捷指令 App 跑「侧边菜单」App Shortcut（与操作按钮同一条系统意图链路）冷启动 / 前台都弹菜单、再跑一次选中项轮换到「分享」。注意模拟器默认 ad-hoc 签名会被 linkd 以 `requiresValidBundle` 拒掉（弹「无法运行 App 快捷指令」，日志 `Couldn't find AppShortcutsProvider`），要 `build-for-testing` 后用开发证书重签 .app 再 `test-without-building` 才能跑到 | 意图投递（`SideKeyCommandBus.post`）与首页消费各记一行 `sideKey:` 诊断，下次日志能分清是快捷指令没到 App 还是 App 没响应。App 已不接相机控制键，操作按钮须在 设置 → 操作按钮 → 快捷指令 里绑 Usage Limits 的「侧边菜单」 | 第二次反馈「菜单点击不生效」后模拟器复现：只有**选中态**胶囊点不到（未选中的都正常），点击穿到遮罩触发 dismiss 收起菜单。按坐标扫点：只有正压在图标 / 文字字形上的点击有效，胶囊空白处（内边距区域）全部穿透。根因：iOS 26 `.glassEffect` 由 `GlassEffectContainer` 渲染、不像 `.background` 那样参与命中测试，胶囊没声明 `contentShape`，可点区域只剩字形；选中项还带 1.05 缩放，字形位置更难对上，所以「有时能点有时不能」。处理：胶囊在玻璃效果前加 `.contentShape(Capsule())`；`--open-side-key` 只弹一次的守卫挪到控制器；`handleSideKey` 每个效果记一行 `sideKey: effect` 诊断 | ✅ 模拟器多点验证，待真机复验 |
| 96 | 真机反馈 0.2.513 小组件 | 点小组件打开 App 后不跳到它展示的那张卡 | 2×2 / 2×4 展示内置主账号时 `widgetURL` 是 `aiusage://open/<provider>`，App 端 `AppDeepLink` 只认 `open/account/<uuid>`（D10 当时只做了附加 / 自定义账号），主账号链接被当成总览、什么都不做 | `AppDeepLink` 新增 `.provider`，`revealTarget(accounts:demoMode:)` 按当前账号表换算成该服务商主账号卡（没主账号退到任一账号卡，演示态退到演示卡）；首页统一按场景项 id 聚焦 / 展开。模拟器 `simctl openurl aiusage://open/openai`：转盘转到 ChatGPT 并展开、平铺滚到 Grok 并展开 | ✅ 待真机复验 |
| 97 | 真机 0.2.529 设置 → 侧边键 | 「前往设置」只到设置首页，到不了「操作按钮」页 | 模拟器 iOS 26.5 抓设置 App 日志（`log stream` 过滤 `URL Handling`）：第三方发来的任何带路径 `App-prefs:<X>`（`ACTION_BUTTON`、`root=ACTION_BUTTON`、`com.apple.settings.actionButton`…）都被 `SettingsAppModel.handleURL` 改写成 `settings-navigation://com.apple.Settings.Apps/<X>`，即「App 列表 → X」，真机上落在 App 列表页；页面真正的地址 `settings-navigation://com.apple.Settings.ActionButton` 与 `prefs:` 都是 `CFBundleURLIsPrivate` scheme，LaunchServices 直接拒绝（错误 115）。Apple DTS 论坛确认 iOS 26 起第三方只有 `openSettingsURLString` / 通知 / 默认 App 三个公开入口，其余 `App-prefs:` 用法不受支持（审核 2.5.1） | 改发不带路径的 `App-prefs:`：设置 App 不改写、不导航，稳定停在首页（「操作按钮」第三行）；弹窗第一步改成「点前往设置后在首页点操作按钮」；新增 `--open-side-key-guide` | ✅ 待真机 |
| 99 | 用户要求 0.4.546 小组件 | 去掉画廊里的 2×4「用量总览」（一个账号、首页前 4 条） | #63 起 2×4 总览只剩单实例，与「单账号多级用量」是同一张卡（#64 已因此合并预览） | 删掉 `OverviewWidget` / `OverviewMediumTimeline`，`OverviewConfigIntent` 只声明 4×4 尺寸，画廊文案 `widget.gallery.overview*` 随之删除；总览只剩 4×4。桌面上已放的 2×4 总览会被系统移除，改放单账号 2×4 即可 | ✅ |
| 100 | 真机截图 0.4.546 小组件编辑页 | 单账号 2×4 编辑页把账号换成 Claude 后，「额度」列表仍是 Grok 的「本周限额 / Grok Build / 额度」；退出编辑后小组件显示的却是 Claude 的额度 | 模拟器给扩展加 os_log 跟到的链路：换账号时系统把额度参数**重置成 `@Parameter(default:)`**，在扩展里现算这些默认实体的 `displayRepresentation`，连文案一起存进小组件配置；之后再开编辑页直接读存下的文案，不再问扩展（换 App 语言、重启模拟器都不变）。旧代码的默认值按 `account: nil` 取「首页第一个可见实例」（Grok）的额度名当标题，`displayRepresentation` 又按 nil 账号回落到同一个实例，所以列表永远是别家的名字；时间线按所选账号解析槽位，小组件本身没错 | 占位槽标题留空，显示时按槽位命名「首页额度 N」（`WidgetEditorPrefill.slotTitle`，5 语），不再按任何实例猜；真实额度名只在 `entities(for:)` 拿到账号依赖后填入，用于时间线映射和「+」添加列表——编辑页已选的槽位行始终显示「首页额度 N」，存下的文案也不随 App 语言切换刷新（系统行为）。顺带把「最高用量（自动）」的编辑页目录改成和时间线挑同一个实例（原先退到首页第一个账号，槽位映射出的是别家的额度 id）。模拟器验证：新加 2×4 → 编辑页「首页额度 1–4」；换成 Claude / Grok 列表不变；退出后小组件分别是 Claude 3 条（Current session / All models / Fable）和 Grok 3 条（本周限额 / Grok Build / Imagine）。注意模拟器 ad-hoc 签名下 linkd 拒掉扩展（`Unable to get teamId`），`entities(for:)` 永远不会被调、时间线拿到的是默认配置（账号回落「最高用量（自动）」、额度为空），要按 #95 的办法用开发证书重签 .app 再 `simctl install` 才能验证配置链路 | ✅ 模拟器验证，待真机复验 |
| 101 | 真机 0.4.548 小组件编辑页 | 所有小组件点「账号」都显示「无可用选项」 | #100 收尾用 sed 清扩展里的诊断日志时，把 `AccountChoiceQuery` 里同样是单行的 `suggestedEntities()` / `defaultResult()` 一起删了；`EntityQuery` 对 `suggestedEntities()` 的默认实现返回空数组，编译照样通过，Core 契约测试也没盯这两个方法。模拟器上没发现是因为验证流程在清日志之前 | 补回两个方法；`WidgetChromeTests` 新增契约：小组件源码里每个 `EntityQuery` 都必须显式写 `suggestedEntities()` / `entities(for:)` / `entities(matching:)`，`AccountChoiceQuery` 还必须有 `defaultResult()`。教训：批量清诊断行后逐行看 `git diff` 的删除行，不能只看编译和测试 | ✅ 模拟器验证，待真机复验 |
| 102 | 真机 0.4.548 小组件编辑页 | 「额度」列表与所选实例不匹配，还出现「额度」这种统配名 | 截图里的四行是 0.4.546 之前按「首页第一个实例」预填并被系统持久化的旧文案（第 3、4 行是旧代码超出实例额度数时写死的「额度」）；#100 改成的四行「首页额度 N」在用户眼里同样是与实例无关的统配名。系统换账号必重置成 `@Parameter(default:)`，默认值拿不到账号，所以任何「N 行额度名」都做不对 | 默认只留一行与账号无关的「跟随首页额度」（`WidgetEditorPrefill.followHomeID` = `home.follow`，`widget.followHome` 5 语），时间线把它展开成所选账号的首页可见额度；要指定额度就删掉它、用「+」从当前账号目录里挑（`suggestedEntities()` 带账号依赖，名字是对的）；旧配置的 `home.slot.N` 继续按序号映射。顺带修掉同链路一处：`WidgetAccountItems.selectedMeters` 原来只在首页可见项里匹配勾选 id，「+」加进去的未上屏额度（0 用量默认隐藏的）根本显示不出来，现改成在所有带数值的额度里找。已放的小组件再开编辑页仍显示存下的旧文案，重新选一次账号才会换成新默认行（系统持久化行为，见 #100） | ✅ 模拟器验证，待真机复验 |
| 103 | 真机 0.4.550 单账号 2×2 编辑页 | 账号 Claude，「额度」列表却是两行「额度」；小组件本身显示 Claude 的 Current session / All models 没错 | 这两行是早期版本（#59 前占位实体 title 写死「额度」）配置小组件时存进系统配置的文案。#100 的日志证实系统对额度这种带 `@IntentParameterDependency` 的数组参数从不调 `entities(for:)`（时间线拿到的就是存下的 `home.slot.N` 原样 id），编辑页也只原样显示存下的文案，扩展无从刷新；账号参数则每次开编辑页 / 选择器都会 `entities(for:)` 重新解析 | 把额度参数从 `metrics` 改名 `quotas`：系统按参数名解码，旧键的值被丢弃、账号照常保留，时间线空值 = 跟随首页（与旧的全占位槽配置结果一致），编辑页对旧小组件显示的是**空列表**（缺键解码成 nil，不走 `@Parameter(default:)`），所以额度参数标题改成专用的 `widget.quotaParam`「额度（留空则跟随首页）」，新加的小组件仍默认一行「跟随首页额度」。代价：老版本里手动勾过额度的小组件退回跟随首页，需要重新勾一次。真机 XCUITest 自测因手机未解锁 / 未开「启用 UI 自动化」报 `认证失败`，没跑成 | ✅ 模拟器验证旧配置换名后账号保留、列表为空且标题说明清楚，待真机复验 |
| 104 | 用户目标 0.4.551 小组件编辑页 | 选任一账号后，「额度」列表要自动带出该账号最多 4 条计量条的真实名字；改名后的旧小组件列表空、换账号只带出一行「跟随首页额度」都不满足 | 模拟器重签包加 os_log 逐条试出换账号时系统的调用顺序：**重建 intent 取 `@Parameter(default:)`（拿不到账号）→ `AccountChoiceQuery.entities(for:)` 回查新账号 → 带新账号依赖调 `MetricChoiceQuery.entities(for:)`（返回值不上屏，只用于时间线解码时裁剪）→ 调默认实体的 `displayRepresentation` 渲染并持久化**。试过的死路：`EntityQuery` 数组 `DefaultValue` + `defaultResult()`（系统会调、带账号，但结果既不上屏也不进时间线，且真机上有 #56 的「无法载入」前科）；单实体 `defaultResult()` 换账号时根本不调；`entities(for:)` 改标题不上屏；静态默认值想按账号定条数也不行（它先于回查执行） | 静态默认值固定 4 个 `home.slot.N` 占位槽；带依赖的查询把账号记进扩展进程内的 `EditorDependencyCache`（只认 2 秒内）；`displayRepresentation` 在 1 毫秒后按这个账号把第 N 槽显示成该账号预填列表第 N 条的名字（首页可见额度在前、目录补齐到 4 条，`WidgetEditorPrefill.prefillIDs`，时间线用同一份映射，编辑页与小组件一致）。账号额度不够 4 条时多余槽位标「（空）」（系统不按 `entities(for:)` 条数裁默认槽，但时间线只显示解析到的）；「最高用量（自动）」/ 拿不到账号叫「首页额度 N」。模拟器验证：Claude → Current session / All models / Fable / Sonnet；Augment → 积分 + 空槽；退出后小组件各显示对应额度；时间线日志 `quotas=["home.slot.0"]` 证明多余槽被裁掉 | ✅ 模拟器验证，待真机复验（UI 自动化被锁屏挡住） |
| 107 | 1.4.553 上架准备 | 分享图二维码扫出来是 App Store 搜索页（`search?term=Usage%20Limits`），同名 App 或改名都可能指错 | `ShareChrome.appStoreURL` 与打包的 `AppStoreQR.png` 都是搜索链接 | 常量改为按 Apple ID 直达 `apps.apple.com/app/id6808912101`，PNG 用同一 URL 重生成（320 px、纠错 M）；新增测试解码包内 PNG 与常量比对，防再次漂移 | 已修，ShareImageTests 27 项通过 |
| 111 | 真机 1.4.557 | App 主题选「深色」，桌面小组件仍是白底 | 小组件底色 `Color(.systemBackground)` 只跟系统外观，从不读 `SharedStore.appTheme`；App 改主题也不 reload 时间线 | `Widget` 三个入口视图加 `widgetAppearance()`：浅色 / 深色固定 `colorScheme` 环境（动态色、底色都按它解析），跟随系统不动；`AppState.theme` didSet 加 `reloadAllTimelines`。契约测试 `LaunchStoreConfigTests` 断言三处挂载与 reload | 已修 |
| 114 | 体验目标 1.4.566 | 登录页打开后，官方站首屏慢时整页空白，只有一句静态提示，看不出页面到底在不在加载 | 登录 WebView 从不观察 `estimatedProgress`，导航期间只有 `didFinish` 一个回调；statusBar 的转圈表示的是「检测登录态」，不是页面加载 | 新增 Core `LoginLoadProgress`（起步给 5% 兜底宽度、满格后 250 ms 再归零、期间开新导航就取消归零），`LoginSheetView` 用 KVO 观察可见登录页的 `estimatedProgress`，在 statusBar 下方画 2pt 品牌色进度条，拆除视图 `invalidate()`；不碰登录检测流程与 WebView 配置。模拟器 DeepSeek 登录页连拍：10% → 17% → 41% → 85% → 100% → 淡出，条高 6 像素（2pt @3x） | ✅ |
| 115 | 体验目标 1.4.567 | 社交登录弹起来是「说明 → 空白 → 蓝色 X → 一片空白卡片」：多一层 chrome 很突兀又占位置，而且这段加载完全没有反馈（OAuth 站点被拦时就一直白着） | `OAuthPopupContainer` 自带 44pt 头 + X 关闭，看着像另开一张卡；顶部加载条只观察主 WebView，弹窗盖上来时主页面不在加载，条不显示 | 容器去掉头与 X，网页铺满容器，视觉上就是登录页自己跳了一步；`presentPopup` 里弹窗上屏即 `trackProgress(of: popup)`，`finishPopup` 拆掉后把观察还给 `page.activeWebView`。退出仍靠站点 `window.close()`、导航策略或工具栏「关闭」。模拟器点 target=_blank 链接复现：说明下方直接是进度条（45.5%）与页面，无空头无 X，加载完条淡出 | ✅ |
| 117 | 1.4.569 分享 | 分享图「明细」开关打开后，套餐行的三枚胶囊底缘贴住首条指标标签，中间没有可见空白 | 原来的 12pt 纯文本在 16pt 行里自带上下留白；胶囊填满整行，行高 16pt 就等于胶囊高，下方零间隙 | 套餐行高度改成按内容取：纯文本仍 16pt，三枚胶囊 20pt（`ShareImageComposer.planRowHeight`），`bodyH` / `cardH` / `ly` 三处同一口径。重出分享图核对 | 已修 |
| 118 | 1.4.570 分享 | 分享预览改成 SwiftUI 真视图后，`ImageRenderer.uiImage` 导出全局分享图恒为 nil，保存到相册与系统分享都拿不到图 | 全局分享画布 390×4593pt，3 倍图是 1170×13779 像素，超出 ImageRenderer 内部的纹理上限 | 改走 `ImageRenderer.render(rasterizationScale:)`，自己开 `CGContext` 让 SwiftUI 往里画，尺寸只受内存限制；导出恢复 1170×13779，与老渲染器同尺寸 | 已修 |
| 119 | 1.4.571 分享 | 新导出图里每张内层服务商卡都多了一圈阴影，老图只有最外层白卡有 | `.shadow` 挂在整棵子树上时 SwiftUI 会让每个子视图各投一份 | 阴影改挂到白卡的背景形状上；同条件 A/B 的像素差异从 5.77% 降到 3.18%，剩余是文字抗锯齿与 1–2px 基线差 | 已修 |
| 120 | 1.4.572 分享 | 分享预览里实例一多，点「明细」整张长图以画布顶边为原点伸缩，正在看的那张卡被上方卡片的高度变化推走，得重新翻找 | 切开关只换 `ShareCardModel`，ScrollView 的 contentOffset 一动不动；上方每张卡一增一减明细行就是几十 pt，二十几个实例累积上千 pt | 新增 Core `SharePreviewScroll` 与 `ShareLayout.sectionTops`：换模型前记下视口中心那张卡、以及它顶边离视口顶边多远，换完按新几何把它推回同一屏幕位置（`scrollPosition.scrollTo(y:)` 与内容高度在同一次动画里推进）；锚点按卡片标题找回，「编辑」改了选择集也认得出；画布外那圈 12pt 内边距按 `slack` 算进可滚范围，停在最顶上时来回换算不会凭空多滚一段 | 已修，`SharePreviewScrollTests` 7 项 |
| 121 | 1.4.574 分享 | #120 落地后锚点仍不管用：点「明细」整张长图先快速上下抽一下再回到原位，而且钉住的是屏幕最上面那张卡，不是中间那张 | 两处独立原因。① 视口高度算错：`ScrollGeometry.containerSize` 已经是扣掉导航栏与底部面板后的净高（iPhone 17 实测 container 523.3 / insets 70 + 218.7 / visibleRect 812），代码又减了一次边距，视口只剩 234.7pt，中心点落到屏幕上沿，锚点永远选到最上面那张卡。② 补偿位移到不了屏幕：`scrollPosition.scrollTo(y:)` 一瞬间落定（geo 日志：一条事件直接从 1600 跳到 1882），卡片重排却要 0.32 秒，中间那段差值本该由一个随动画归零的位移补上；但补偿写成 `ViewModifier` + `.offset`，而 `.offset` 自身也是可动画的，套在动画子树里被外层再插值一次，两次插值互相抵消，`body` 里算出的位移一点也落不到屏幕上（打印看得到值在动，画面纹丝不动）。补偿还依赖「跳多远 + 第几拍」两个状态，其中一个不参与动画，合并出来的事务把这枚修饰器的动画一并抹掉 | 视口改成 `top = contentOffset.y + contentInsets.top`、`height = containerSize.height`；补偿改成 `GeometryEffect`（纯渲染变换，不会被二次插值），只认一个累计状态 `pinBase`，并用 `.animation(morph, value: pinBase)` 显式绑定；画布外框留在补偿与动画之外，可滚范围一瞬间就位，新偏移不会被旧高度夹住。25 个实例停在中段录 60fps 逐帧量：锚点卡顶边 260/260 帧偏差 0px，上一张下移 43px、下方两张上移 42 / 85px；反向切回 236/236 帧 0px 且布局精确复原；停在最顶上连切两次白壳顶边恒在 657px | 已修 |
| 122 | 1.4.575 分享 | 非匀速连点「明细」时，中间卡片突然上下抽动后复位 | 用 70–800ms 不等间隔序列复现；日志中 OpenCode 锚点始终不变，偏移严格在 1600/1360pt 间切换，排除锚点和偏移算错。根因是上一段重排尚未结束就反向改模型，卡片布局与 `AnchorPin` 两条动画的中断插值失步；原有测试只覆盖静态起终点 | `SharePreviewTransition` 将重排串行化，动画期间按钮照常更新选项，`.removed` 完成后只按最新选项接续；相同模型不重复播放。改用 0.32s easeInOut，避免弹簧尾段将交接拖至约 0.86s。新增请求合并、接续中再点击与视图完成回调契约测试 | iPhone 17 / iOS 26.5：原实现录屏复现；修复后中段、顶部、后段、底部共 96 次切换（10–800ms），中段 60fps 连续 360 帧顶边恒为 1149px，未再抽动。Core 1113 项全绿，App scheme 编过。临时压力入口与日志已移除；真机未跑 |
| 123 | 1.5.576 分享清理 | 切换预览选项重复读取图标，测试仍模拟已经移除的绘图入口 | 每次重排都创建 ShareCardAssets；ComposedShare 兼容层保留未使用的位图参数，部分断言只比较包装器常量 | 复用预览资源，自定义图标仅在图片数据变化时解码；预览复用 ShareFlow.model，导出直接返回 PNG，删除 ShareResult / payload 和旧测试包装，测试直接检查模型与 ShareLayout，压缩历史性注释 | Core 与 App 编译通过，模拟器导出验证；版本同步与发布记录见对应提交 |
| 124 | iPad 适配 | App 只声明 iPhone、竖屏、全屏；装到 iPad 会以 iPhone 兼容模式放大显示，横屏 / 分屏 / Stage Manager 都不支持 | 设备家族 "1"、`UIRequiresFullScreen`、所有页面按手机宽度铺满 | 四条线并行改：① project.yml 设备家族 1,2、iPad 四向、去全屏（App/Info.plist 由 xcodegen 生成，契约测试改为新口径）；② 平铺主题 regular 宽度 LazyVGrid 自适应多列（360–460pt，≤3 列，卡片 `.top` 对齐保住原位展开），拖动排序加二维判定并在单列 regular 时退回纵向口径；轮盘 / 螺旋卡宽只在 regular 宽度按场景高度放大（上限 520，iPhone Plus / Max 仍是 compact，取值不变）；侧边键菜单锚点改用 App 窗口高度；③ 新增 `ReadableWidth` 修饰器（regular 700pt 居中）套设置各页、各 sheet、诊断页，小组件预览在 regular 封顶原生尺寸；④ 登录页与分享预览 `.presentationSizing(.page)`，分享面板改宿主控制器自 present 并给 popover 锚点（iPad 上无锚点会崩），`showing` 按 presentedViewController 校正防卡死；UA 保持不变（各家探针与解析按现有 UA 校准，换 UA 等于一次动 25 家口径）。审查抓到并已修：Plus / Max 卡宽误放大、单列 regular 拖动失灵、分享 `showing` 卡死路径。本轮遗留问题见 #125 / #126 / #127，均已修；上架仍需补 iPad 商店截图位 | iPad Pro 13 / iPhone 17 编译通过，Core 1087 项通过；iPad Pro 13 十二页竖 / 横屏截图核对 |
| 125 | 审查 1.4.574 | iPad 平铺两列时，把左边的卡拖到同排右边那张卡的右半区，卡片纹丝不动（两张卡、2×2 都复现） | `gridDecision` 判断「参考卡这一行有没有邻居」时用的是 `others`（已剔除被拖卡）。两列各一张时，参考卡唯一的同行邻居恰好就是被拖的那张，于是被判成单列、改用上下中线，指针又正好停在中线上 → 返回 `.none`。同行判断还只看纵向重叠，没看是不是另一列 | 算法整体搬到 Core `GridDragReorder`（纯数值几何，不依赖 SwiftUI），同行判断改为在**含被拖卡的全部卡**里找「纵向正重叠且横向属于另一列」的邻居；排除被拖卡只用于算目标插入位。App 侧删掉私有副本，`applyPointer` 按 `accountOrder` 取 `cardFrames` 转成 Core 模型。另加护栏：指针已落在被拖卡自身的新位置时返回 `.none`，否则重放同一指针会把参考卡退化成斜对角邻居、顺序继续往后滑 | 新增 `GridDragReorderTests` 19 项（两列 / 2×2 / 三列 / 单列 regular / 展开高度不等 / 滞回带 / 退化输入 / 重放稳定性）；回退到修复前逻辑复跑，7 个用例 9 处断言失败，确认锁住修复。iPad Air 11 M4 用 `--seed-demo-snapshots` 造真实账号实测：同排左→右、右→左都生效，重启后顺序持久化 |
| 126 | 审查 1.4.574 | iPad 窗口宽度跨过 regular / compact（窗口化 App 缩放、分屏）时，页面滚动位置回到顶部 | `ReadableWidthModifier.body` 是 `@ViewBuilder` 的 if / else，两条分支各含一份 `content`，生成 `_ConditionalContent`。size class 一翻转整棵子树销毁重建，`List` 滚动位置、`@FocusState`、子视图 `@State` 全部归零 | `content` 收到唯一一条结构路径上，宽度靠 `horizontalSizeClass` 改数（compact 放 `.infinity`），只有背景保留条件分支（不含 `content`，重建不丢状态）。同时删掉注释里「不插入任何布局节点、逐像素一致」的过度承诺 | iPad Air 11 M4（窗口化 App）实测：修复前把服务商列表滚到中部再缩窄窗口直接弹回顶部；修复后 regular→compact、compact→regular 两个方向都停在原位置。表单 sheet（颜色编辑器）本来就一直是 compact，未提交的 `1122` 与输入焦点跨两次缩放都在，修复前后一致 |
| 127 | 审查 1.4.574 | 首页平铺在多列 / 单列之间切换时，阅读位置回到第一张卡 | `LazyVGrid` 与 `LazyVStack` 是两个容器，size class 翻转时整段内容重建，`ScrollView` 偏移归零。偏移量本身没法沿用——两列的 y 和单列的 y 不是一回事 | 新增 Core `DashboardScrollAnchor`：每张卡只上报自己相对视口顶边的三段位置（`onGeometryChange` 只在取值变化时回调，不逐像素写状态），压住顶边的那张就是锚点；首张卡回到顶边下方时清空锚点，免得把横幅顶出视口。`usesMultiColumn` 一变就滚回锚点，滚两次（懒容器要滚过去才建出沿途的卡，展开的卡一变高第一次会短一截）。展开状态仍由 `expandedIDs` 保管，不新开状态源 | 新增 `DashboardScrollAnchorTests` 12 项。iPad Air 11 M4 实测：修复前两列滚到 LongCat / MiMo 那行缩窄窗口直接弹回 Claude；修复后停在 DeepSeek 那张（锚点卡）|
| 128 | 审查 1.4.574 | `safeContentFrame` 的注释说 iPad 上「也照样拿 390×844」，与即梦接管页的实际行为不符 | 注释只描述了构造时的初始 frame。可见登录页是 `UIViewRepresentable`，上屏即被 SwiftUI 按 sheet 尺寸重排；即梦接管页的 frame 被改成 key window bounds 并挂 autoresize，跟着 iPad 窗口缩放 | 注释改成分三类写（离屏探针固定 390×844 / 可见登录页由 SwiftUI 分配 / 即梦跟随窗口）；`SafariUserAgent` 去掉「所有页面和接口必然移动版」的说法；`providers/README.md` 补「视口与 UA」表，`providers/jimeng.md` 记下 iPad 上的实测与待验证项。不改 UA、不改回隐藏小窗、不加 URLSession 官方站请求 | 模拟器未登录打开即梦 `/ai-tool/home`（iPad Air 11 全屏竖屏，视口 820pt）拿到的是桌面版布局；桌面版 SSR 是否照写 `__isLogined` / `webSignBody` **待真机真实账号验证**，不得据此断言即梦在 iPad 上可用或不可用。ChatGPT 登录页在全屏竖 / 横屏 / 窄窗三种形态渲染正常、三个社交按钮齐全，真实登录同样待验证 |
| 129 | iPad 整合 1.5.583 | iPad 分支与 master 的分享预览、版本、开发日志发生冲突 | 分支仍使用旧位图预览，master 已改为实时画布并修复连续切换时的滚动补偿 | 保留 master 实时画布、资源复用与 SharePreviewTransition，加入 iPad page sheet、可读栏宽与系统分享 popover；保留两边日志并重排编号。版本累计 1.5.576 + 7 = 1.5.583，不创建 commit | Core 1145 项全绿（含 LoginConfirmTests）；xcodegen、iPad Debug 与 iPhone Release scheme 构建通过；模拟器横竖屏、分享面板重复打开、设置及小组件预览检查通过。真机登录、即梦抓取、窗口缩放等未验项详见 docs/plans/2026-09-08-ipad-master-integration.md |

### 2026-09-09 · 1.5.584 · Gemini HTML 错误归因

- 用户日志：`gemini.quota` 连续 HTTP 200、约 850 KB，解析为「配额数据异常」。现有探针请求 `/app` 网页，占位接入没有已验证的 Cookie 配额 API；字节数不能证明发生了 JSON 截断。
- 修复：识别 HTML 开头，明确提示网页用量暂未接入、重新登录不能解决；保持错误状态且不确认登录、不编造额度。该错误不覆盖 last-good；401/403 和其他 HTTP 错误仍按原规则处理。五种语言文案齐全，接口目录同步。
- 回归：合成 HTML 夹具、BOM / 空白 / 大小写、HTTP 错误优先、损坏 JSON、登录判定与 last-good；修复前 4 处断言失败，修复后 Core 1149 项全绿（含 LoginConfirmTests）。
- 边界：本轮修复错误提示与缓存保护，未接通真实 Gemini 网页 usage。真机 Google 登录、Cookie / CORS 与真实配额响应尚未验证；需先验证官网同源端点，禁止用桌面 OAuth / CLI 配额替代网页用量。
- 工程验证：`xcodegen generate` 成功；UsageLimits scheme 在 iPhone 17 / iOS 26.5 Simulator destination 构建成功，未做真实账号设备验证。

### 2026-09-09 · 1.5.585 · Gemini 官网 GetUsageInfo 接入

- 用户提供 `/u/3/usage`，通过 Chrome DevTools 导出网络样本，定位 `POST /u/<index>/_/BardChatUi/data/batchexecute?rpcids=jSf9Qc`；官方前端将其标为 `BardFrontendService.GetUsageInfo`。前端 `Chm` 证实窗口类型 1/2、已用比例字段与秒/纳秒重置时间；不是旧 remainingFraction 桶，也不能用内部容量值计算百分比。
- 探针从同源 usage 引导页的具名 WIZ JSON 取 CSRF / session 参数，再 POST RPC；不依赖 defaultClient 不可见的页面全局。可见页优先当前 Google 账号路径，离屏读取同 dataStore 的已成功路径；账号 ID 仅在源内哈希，沿用 AccountIdentity 防串号。HTML、RPC 无有效数据不覆盖 last-good，真实 401/403 保持原语义。
- Chrome 验证：原始参数重放与重新 fetch 引导 HTML 两种方式均 HTTP 200，返回当前 5 小时 / 每周 0%，与官网一致。捕获样本只保留脱敏配额夹具；CSRF、完整 HAR / HTML 未进仓库。
- 测试：新 RPC 回归在旧实现上 13 处断言失败；新增生产 JS 执行、多账号路由、失败不缓存、身份脱敏、无效字段与百分比口径测试。Core 1157 项通过（含 LoginConfirmTests），xcodegen 与 UsageLimits scheme 编译通过。
- 模拟器闭环：iPhone 17 Pro / iOS 26.5 安装 1.5.585，复用已有 WebKit Cookie；10:44:35Z 可见页检测得到 `ok` 两条指标，点击保留的登录确认后 10:45:24Z 落盘。18:46:23 冷启动 App，10:47:16Z 离屏刷新再次 `ok`，226 字节，Google AI Pro / five_hour 0% / weekly 0%，重置时间已持久化。物理真机、新账号 Google OAuth 全流程未跑。
- 未创建 commit；base/pub 同步脚本要求已提交且干净的 master 并自动提交快照，因此本轮未运行同步。
# 2026-09-09 · 1.5.586 · 接入目录核验

- 保留用户确认的 11 家接入；其余 14 家官方公开资料不足以验证当前 App 同源接口与鉴权，暂时隐藏。逐家依据见 `providers/availability.md`，不宣称官网没有用量功能。
- 独立 `ProviderAvailability` 门禁覆盖添加目录、账号设置、首页、提醒设置、预览、小组件和手表，以及前后台自动刷新。ProviderID、账号、Cookie、开关、缓存均保留；可见列表拖动保留隐藏账号原位置，自定义账号不受影响。
- Core 1161 项全绿；包含 LoginConfirmTests、隐藏账号不展示/探针、可见拖动保留隐藏记录、自定义账号和预览门禁。XcodeGen 与 UsageLimits 模拟器 App scheme 编译通过。新供应商真实登录、真机 Widget / Watch 与后台刷新仍需设备验证。

### 2026-09-10 · 1.5.587 · 新增繁体中文

- `AppLanguage` 加 `zh-Hant`（rawValue 就是 `zh-Hant`，持久化与手表同步共用这一串），设置 → 外观的语言选择器自动多出「繁體中文」；原来的「中文」改称「简体中文」，否则两项同名分不清。
- 字符串表 529 条全部补繁体，按台港澳用词改写而非简转繁直译：账号→帳號、登录→登入、刷新→重新整理、小组件→小工具、设置→設定、令牌→權杖、积分→點數、阈值→門檻、快捷指令→捷徑、相册→照片、复制→拷貝、代金券→抵用券、限流窗口→時段。侧边键指引里快捷指令自身的名字「侧边菜单」在系统里未本地化，各语言一律原样保留。
- 跟随系统的匹配改成按脚本子标签分繁简：`zh-Hant-*` / `zh-TW` / `zh-HK` / `zh-MO` 判繁体，`zh-Hans-*` / `zh-CN` / `zh-SG` / 裸 `zh` 判简体，其余语言仍按前缀匹配、匹配不上回落英语。原实现按 rawValue 前缀顺序遍历，`zh-Hant` 永远会被 `zh` 抢先命中，加了枚举也不会生效。
- 同步补齐 `App/zh-Hant.lproj/InfoPlist.strings`（相册权限文案）与 `Widget/Localizable.xcstrings` 的 19 条系统串（小组件画廊与配置意图参数）。App 和小组件 bundle 都必须声明 zh-Hant，否则系统不会把繁体用户的首选语言解析成 zh-Hant，跟随系统那条路走不通。
- 新增 `LocalizationTests` 契约：六种语言必须齐、繁体值不得含简体专有字形（快捷指令原名除外）、繁简占位符一致、中文文案繁简差异率 > 70%（防「把简体整表复制过去」假补齐）、系统语言映射 20 例。故意把一条繁体改成简体、再故意删掉一条繁体，各跑一次确认两条断言分别命中后还原。测试里 26 处硬编码语言数组统一改成 `AppLanguage.concrete`，以后加语言不用再逐处补。
- 结果：Core 1172 项全绿，`xcodegen generate` 与 UsageLimits scheme 构建通过。iPhone 17 / iOS 26.5 模拟器逐页核对首页、设置、外观、提醒、服务商、小组件预览、分享预览、自定义向导、侧边键指引；Apple Watch Ultra 3 模拟器核对用量环与设置。跟随系统（`-AppleLanguages "(zh-Hant-TW)"`）与显式选繁体两条路径都正确，简体各页逐张比对无回归。
- 剥离自查：scrub 到临时目录预演一遍，繁体条目完整保留、二次剥离零改动；剥离后的 Core 除 `RepoHygieneTests`（需要 git 仓库才能跑）外全绿。
- 边界：App Store Connect 的商店文案还没加繁体本地化；真机未验证。

## 2026-09-10：1.5.588 ChatGPT / Codex 重置摘要

- 接入同源只读 `rate-limit-reset-credits` 与 `/history`，4 秒补充探针；卡片显示可用次数、最近到期、时间范围内的已使用次数。历史去重，分页未完整或数据漂移时显示“至少”；不推算新的对话额度。
- 摘要独立于 metrics，兼容旧快照；不存事件 ID、头像、用户字段，诊断不预览响应正文。补充接口不参与登录判定、额度阈值与 last-good HTTP 状态聚合。
- 审查中复现“主探针全部超时/503、补充接口200/401”使旧快照丢失的四种组合，新增回归后修复；场景折叠卡改为单行摘要，避免固定高度裁切。仅重置摘要的卡片也可展开。
- 验证：用户现有 Chrome 登录页同源 GET 两接口均200，可用1次、到期2026-10-05 04:21 UTC，近30天 used 1条、granted 2条、无下一页。仅读取，没有兑换或购买。Core 1179项全绿（含 LoginConfirmTests），XcodeGen 与 UsageLimits scheme 构建通过；iPhone 17 / iOS 26.5 模拟器核对平铺展开、轮盘/螺旋折叠显示，独立代码复核通过。
- 设备边界：未进行真机 WKWebView 登录与 Cookie/CORS 实测；Chrome 会话验证不等同于真机验证。未实测 Widget/Watch（摘要不进入其额度展示）。
